# 园丁工作台 · 系统架构说明文档

## 1. 架构总览

### 1.1 系统上下文图 (C4 Context)

```mermaid
graph TB
    subgraph "用户"
        T["👩‍🏫 教师"]
        A["👔 校管"]
        SA["🛡️ 超管"]
        P["👪 家长"]
    end

    subgraph "园丁工作台"
        SYS["园丁工作台系统"]
    end

    subgraph "外部依赖"
        WX["微信小程序平台"]
        AI["AI 大模型服务"]
        DB[(MySQL)]
    end

    T -->|"Web / 小程序"| SYS
    A -->|"Web 管理端"| SYS
    SA -->|"Web 管理端"| SYS
    P -->|"小程序家长端"| SYS
    SYS -->|"云函数调用"| WX
    SYS -->|"API 调用"| AI
    SYS -->|"数据持久化"| DB
```

### 1.2 技术架构分层图

```mermaid
graph LR
    subgraph "表现层"
        WEB["Web Vue3"]
        MP["小程序 uni-app"]
    end

    subgraph "接入层"
        ROUTE["路由守卫"]
        AUTH["JWT 鉴权"]
        LIMIT["限流"]
    end

    subgraph "业务层"
        AI_SRV["AI 服务"]
        GRADE["成绩管理"]
        CLASS["班级管理"]
        STU["学生管理"]
        EXAM["考试管理"]
        TOOL["学科工具"]
    end

    subgraph "数据层"
        ORM["TypeORM"]
        CACHE["LRU Cache"]
        MYSQL[(MySQL)]
    end

    WEB --> ROUTE
    MP --> ROUTE
    ROUTE --> AUTH
    AUTH --> LIMIT
    LIMIT --> AI_SRV
    LIMIT --> GRADE
    LIMIT --> CLASS
    LIMIT --> STU
    LIMIT --> EXAM
    LIMIT --> TOOL
    AI_SRV --> ORM
    GRADE --> ORM
    CLASS --> ORM
    STU --> ORM
    EXAM --> ORM
    ORM --> MYSQL
    AI_SRV --> CACHE
    GRADE --> CACHE
```

---

## 2. 后端架构 (NestJS)

### 2.1 模块依赖图

```mermaid
graph TD
    APP[app.module.ts] --> AUTH_MOD[auth.module]
    APP --> AI_MOD[ai.module]
    APP --> GRADE_MOD[grades.module]
    APP --> STU_MOD[students.module]
    APP --> CLASS_MOD[classes.module]
    APP --> EXAM_MOD[exams.module]
    APP --> COMMON_MOD[common.module]
    APP --> FEATURE_MOD[feature.module]

    AI_MOD --> CHAT[ai-chat.service]
    AI_MOD --> AI_SVC[ai.service]
    AI_MOD --> PARSER[ai-file-parser]
    AI_MOD --> VISION[ai-vision]
    AI_MOD --> MEDIA[ai-media]

    COMMON_MOD --> CACHE_MOD[cache.module]
    COMMON_MOD --> CRUD_BASE[base.service]
    COMMON_MOD --> GUARDS[jwt-auth.guard]
    COMMON_MOD --> FILTERS[exception.filter]

    GRADE_MOD --> GRADE_SVC[grades.service]
    GRADE_MOD --> CHAT

    CLASS_MOD --> CLASS_MEM[class-members]
```

### 2.2 请求处理流水线

```mermaid
sequenceDiagram
    participant Client as 客户端
    participant Guard as JWT Guard
    participant Rate as Rate Limit
    participant Ctrl as Controller
    participant Svc as Service
    participant ORM as TypeORM
    participant DB as MySQL
    participant Cache as LRU Cache

    Client->>Guard: HTTP请求 + Bearer Token
    Guard->>Guard: 校验 JWT 签名/过期
    Guard->>Rate: 通过鉴权
    Rate->>Rate: 检查配额 (内存计数)
    Rate->>Ctrl: 配额充足
    Ctrl->>Svc: 调用业务逻辑
    Svc->>Cache: 查询缓存
    alt 缓存命中
        Cache-->>Svc: 返回数据
    else 缓存未命中
        Svc->>ORM: QueryBuilder
        ORM->>DB: SQL
        DB-->>ORM: ResultSet
        ORM-->>Svc: Entity[]
        Svc->>Cache: 写入缓存
    end
    Svc-->>Ctrl: DTO
    Ctrl-->>Client: JSON Response
```

### 2.3 缓存架构

```mermaid
graph LR
    subgraph "CacheService (lru-cache)"
        LRU["LRU Cache<br/>maxSize: 200MB<br/>maxAge: 可配"]
    end

    subgraph "缓存作用域"
        TENANT["租户级<br/>(schoolId)"]
        TEACHER["教师级<br/>(teacherId)"]
        GLOBAL["全局级<br/>(如配置)"]
    end

    SVC["业务服务"] --> LRU
    LRU --> TENANT
    LRU --> TEACHER
    LRU --> GLOBAL

    INVALIDATE["主动失效<br/>(EntitySubscriber)"] -.-> LRU
```

### 2.4 AI 服务架构

```mermaid
graph TD
    CHAT_CTRL[ai.controller] --> CHAT_SVC[ai-chat.service]
    CHAT_SVC --> CIRCUIT[CircuitBreaker<br/>熔断器]
    CHAT_SVC --> RETRY[ExponentialBackoff<br/>指数退避]
    CHAT_SVC --> SEM[Semaphore<br/>并发限制]
    CHAT_SVC --> CONTEXT[Context Builder<br/>上下文注入]

    CHAT_SVC --> AI_PRV["AI Provider<br/>(OpenAI 兼容)"]
    PARSER[ai-file-parser] --> AI_PRV
    VISION[ai-vision] --> AI_PRV
    MEDIA[ai-media] --> AI_PRV

    AI_PRV --> MODEL["大模型<br/>gpt-4o / claude / deepseek"]
```

---

## 3. 前端架构 (Vue 3)

### 3.1 应用初始化流程

```mermaid
sequenceDiagram
    participant App as App.vue
    participant Init as initMonitor()
    participant Auth as auth-machine
    participant Router as Vue Router
    participant API as request.ts

    App->>Init: 启动监控
    App->>Auth: 恢复登录态 (冷启动)
    alt 存在有效 token
        Auth->>API: 获取功能包配置
        API-->>Auth: features JSON
        Auth->>Auth: applyFeatureProfile()
    else 无 token
        Auth->>Router: 跳转 /login
    end
    Auth->>Router: 就绪，渲染布局
```

### 3.2 状态管理架构

```mermaid
graph TD
    subgraph "Pinia Stores"
        AUTH_STORE["auth.store<br/>(token, user, role)"]
        PREFS_STORE["prefs.store<br/>(fontSize, theme)"]
        AUTH_MACHINE["auth-machine<br/>(登录方法, features)"]
    end

    subgraph "Composables"
        USE_CLASSES["useClasses<br/>(班级列表+invalidate)"]
        USE_CODING["useCodingEngine<br/>(少儿编程)"]
    end

    subgraph "SWR 缓存"
        REQ_CACHE["request.ts<br/>(cachedGet + invalidate)"]
    end

    COMP["组件"] --> AUTH_STORE
    COMP --> PREFS_STORE
    COMP --> AUTH_MACHINE
    COMP --> USE_CLASSES
    COMP --> REQ_CACHE
```

### 3.3 路由守卫流水线

```mermaid
flowchart LR
    A[导航触发] --> B{已登录?}
    B -->|否| C[跳转 /login]
    B -->|是| D{角色匹配?}
    D -->|否| E[跳转 403]
    D -->|是| F{功能包允许?}
    F -->|否| E
    F -->|是| G[加载页面组件]
    G --> H[渲染布局]
```

---

## 4. 小程序架构 (uni-app)

### 4.1 分包策略

```mermaid
graph TD
    subgraph "主包 (2MB)"
        LOGIN[login]
        DASH[dashboard]
        TOOLBOX[toolbox]
        CONFIG[config]
    end

    subgraph "子包: games (预加载)"
        G1[snake/2048/sudoku]
        G2[breakout/tetris]
        G3[其他 30+ 游戏]
    end

    subgraph "子包: tools (预加载)"
        T1[dictation/pinyin/idiom]
        T2[math/grammar/listening]
        T3[AI tools x9]
    end

    subgraph "子包: ai-features"
        AI1[ai-chat / ai-exam]
    end

    subgraph "子包: teaching"
        TEA[grades/exams/schedule]
    end

    subgraph "子包: community"
        COM[im/notice/gallery]
    end

    subgraph "子包: parent"
        PAR[dashboard/report]
    end

    subgraph "子包: school-admin"
        SA[dashboard/config]
    end

    DASH -->|"navigateTo"| G1
    DASH -->|"navigateTo"| T1
    TOOLBOX -->|"switchTab"| T1
```

### 4.2 鉴权状态机

```mermaid
stateDiagram-v2
    [*] --> 未登录
    未登录 --> 冷启动恢复: App.onLaunch
    未登录 --> 已登录: login()
    冷启动恢复 --> 已登录: token 有效
    冷启动恢复 --> 未登录: token 失效
    已登录 --> 未登录: logout()
    已登录 --> 角色切换: switchRole()
    角色切换 --> 已登录: 切换完成
```

---

## 5. 数据库架构 (TypeORM)

### 5.1 核心实体关系

```mermaid
erDiagram
    SCHOOL ||--o{ TEACHER : has
    SCHOOL ||--o{ STUDENT : has
    SCHOOL ||--o{ CLASS : has
    CLASS ||--o{ STUDENT : contains
    CLASS ||--o{ CLASS_MEMBER : has
    CLASS ||--o{ EXAM : has
    EXAM ||--o{ GRADE : has
    STUDENT ||--o{ GRADE : has_scores_in
    TEACHER ||--o{ CLASS_MEMBER : joins
    TEACHER ||--o{ NOTE : creates
    STUDENT ||--o{ AWARD : receives

    SCHOOL {
        string id PK
        string name
        json features
    }
    TEACHER {
        string id PK
        string name
        string phone
        string schoolId FK
    }
    STUDENT {
        string id PK
        string name
        string classId FK
        string schoolId FK
    }
    CLASS {
        string id PK
        string name
        string teacherId FK
    }
    EXAM {
        string id PK
        string name
        date date
        string classId FK
    }
    GRADE {
        string id PK
        string subject
        json scores
        string examId FK
    }
```

### 5.2 关键表索引策略

| 表 | 索引名 | 列 | 用途 |
|----|--------|-----|------|
| grades | idx_grades_class_date | (classId, date) | 班级成绩查询 |
| grades | idx_grades_exam | (examId) | 考试成绩关联 |
| students | idx_students_class | (classId) | 班级学生列表 |
| exams | idx_exams_class_date | (classId, date) | 考试时间排序 |
| teacher | idx_teacher_school | (schoolId) | 校内教师查询 |

---

## 6. 跨端共享架构 (@gardener/shared)

```
shared/
├── utils/
│   ├── sse-parser.ts        # SSE 流式解析器（两端共用）
│   ├── format.ts            # fmt1/pct/scoreColor 成绩格式化
│   └── security.ts          # isSessionInvalid 会话失效判定
├── schemas/
│   ├── subject-schema.ts     # 学科工具校验
│   └── crud-schema.ts       # CRUD 参数校验
├── constants/
│   ├── subjects.ts           # 学科常量定义
│   └── features.ts           # Feature Flag 键名
├── types/
│   └── *.ts                  # 共享类型定义
└── validators/
    └── *.ts                  # 校验函数
```

### 跨端调用方式

```
三端 tsconfig/vite paths:
@gardener/shared/*  →  shared/src/*

构建时直接编译共享库源码，无需预构建。
```

---

## 7. 关键设计决策记录

### 7.1 为什么用 LRU 替代 Redis？

微信云托管环境下 Redis 需独立实例 + 额外费用。进程内 LRU 满足：
- TTL 过期
- 作用域失效（租户/教师）
- 200MB 上限

代价：多实例缓存不共享、重启丢失。通过主动失效 + DB 兜底缓解。

### 7.2 为什么关闭 DB synchronize？

生产环境禁止 TypeORM 自动建表（避免误删列）。表结构变更通过手动 SQL 迁移 (`server/migrations/*.sql`)。

### 7.3 为什么 AI 上下文注入？

提升 AI 对话质量，让模型了解教师所教班级、学生、近期考试。30s TTL 缓存避免每次对话都查库。

### 7.4 为什么游戏单独分包？

小游戏页面数量大（35+），独立分包避免撑大主包，符合小程序 2MB 主包限制。

---

## 8. 技术债务与演进方向

| 债务 | 影响 | 演进方向 |
|------|------|----------|
| 进程内限流不准 | 多实例下配额翻倍 | 引入 Redis 计数 |
| scores JSON 聚合慢 | 大数据量时统计慢 | 冗余列 + 生成列索引 |
| 上下文缓存失效延迟 | 数据变更后 AI 仍用旧数据 | 事件驱动主动刷新 |
| 小程序单文件过大 | dashboard 1097 行 | 拆分子组件 |
| AI 成本不可控 | 无限流用量 | 增加班级/教师级配额 |

---

*文档版本: v1.0 | 生成日期: 2026-08-19*
