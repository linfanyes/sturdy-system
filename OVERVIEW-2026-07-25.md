# 2026-07-25 整改与修复总览

## 完成清单

### 1. 测试报告遗留项全部整改（#220–#226）
- **web-app 测试补全**
  - 新增 `test/request.spec.ts`：覆盖请求拦截器、JWT 注入、401 清理 localStorage + 重定向、错误解包（12 例）。
  - 扩展 `test/components/dashboards.spec.ts`：覆盖教师/家长 Dashboard 加载中、空数据、接口错误态（11 例）。
  - 新增 integration、组件、fixtures、helpers 等测试文件。
  - `jest.config.cjs` 增加 `coverageThreshold` 回归门禁（当前基线 lines 55 / statements 55 / branches 40 / functions 45）。
- **server 测试与校验**
  - 新增 `admin`/`parent-auth`/`school-admin` 三处 DTO（`class-validator`），对应控制器全部 `any` 入参改为强类型 DTO。
  - 新增 `BusinessException` 统一异常 + `typeorm-exception.filter` 收敛为 `{ statusCode, code, message }`。
  - 新增 `test/dto-validation.spec.ts`（12 例）、`test/business-exception.filter.spec.ts`（4 例）、`test/tenant-isolation.spec.ts`（10 例：ISO-01~06 / D1）。
- 更新 `web-app/TEST_REPORT.md` 第 8 节，标记所有遗留/建议项为 ✅ 已解决。

### 2. 双侧边栏 bug 修复（#231）
- **根因**：`web-app/src/router/index.ts` 中四个角色路由组（super / school_admin / teacher / parent）的父节点误用 `AppLayout.vue` 作组件，而 `App.vue` 已全局包裹 `AppLayout`，导致布局嵌套、出现两个“园丁工作台”侧边栏。
- **修复**：新增 `src/layouts/RouteOutlet.vue`（仅 `<router-view />`），将四个父路由组件替换为 `RouteOutlet.vue`。
- **验证**：已截图确认四个角色页面均只剩一个左侧边栏，无重复。

### 3. 构建、提交与启动
- **测试**：web-app 14 套件 250 例 / server 11 套件 164 例 / mini-program 6 套件 102 例，共 516 例，0 失败。
- **web-app**：`vue-tsc -b` 通过；Vite dev server 已启动在 http://localhost:5202/。
- **mini-program**：`uni build -p mp-weixin` 成功，产物位于 `dist/build/mp-weixin`。
- **Git**：提交 `cbb1c34` 并推送 gitee master 成功。

### 4. 早先已修复的三项缺陷（已包含在本次提交中）
- web 登录页去掉提示文案。
- 小程序超管页配置/日志 tab 不再显示学校管理员列表。
- 学校管理员密码统一走 bcrypt，`verifyAndUpgrade` 兼容旧 sha256。

## 访问地址
- Web 前端：http://localhost:5202/
- 小程序产物：可导入 `mini-program/dist/build/mp-weixin` 到微信开发者工具。

## 未入库的生成产物
以下文件留在工作区但未 commit，可按需手动处理：
- `web-app/lighthouse-report.html`
- `web-app/lighthouse-report.json`
- `web-app/lighthouse-summary.json`
- `web-app/lighthouse-smoke.png`
- `OVERVIEW-2026-07-25.md`（本文件）
