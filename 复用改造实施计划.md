# 复用改造分阶段实施计划

> 本计划基于 `复用可行性分析报告.md`（2026-03-17）的结论，并**纳入本次补齐功能时新增的复用候选**（小游戏得分、AI 对话历史两端 API 层）。
> 范围：Web 端（`web-app`）、小程序端（`mini-program`）在同一后端（`server`）下的跨端代码复用，目标是在不引入平台依赖的前提下，把重复逻辑收敛进 `shared/`。
> 现状：`shared/`（constants / types / validators）已通过 `@gardener/shared/*` paths 别名被两端引用，零发布流程。

---

## 〇、本次补齐功能产生的"新复用点"

在让 Web 与小程序功能完全一致的过程中，两端新增了**结构平行、逻辑近似**的 API 层。它们是后续复用改造的**首批、最低风险**对象：

| 功能 | Web 层 | 小程序层 | 后端 | 复用机会 |
|---|---|---|---|---|
| 小游戏得分 | `web-app/src/api/games.ts`（submit/fetchList/fetchBest） | `mini-program/src/common/game-score.js`（report/fetchScores/fetchBest） | `server/src/game-scores/*` | 端点契约、`gameKey→显示名`映射、节流策略可共享 |
| AI 对话历史 | `web-app/src/api/chat.ts`（create/fetch/append/pin/delete） | `mini-program/src/common/chat-history.js`（syncSessionToBackend） | `server/src/chat-history/*` | 端点契约、会话/消息 DTO 类型可共享 |

**关键点**：这两块是"同一后端、两端各写一层"的典型。先落地 `shared/api/endpoints.ts`（API 契约），即可让两端同时从同一份类型推导，消除签名漂移。

---

## 一、总体架构原则（沿用可行性报告）

1. **shared 只放无平台依赖的纯 TS**：一旦进入 shared，禁止 import `uni.*` / `window.*` / `document.*` / `wx.*` / `process.*`。
2. **adapter 薄、shared 厚**：两端平台适配层不超过原文件 20%，核心逻辑在 shared。
3. **先用 paths 别名、暂不 npm 发布**：`@gardener/shared/*` 直接引用源码，无预构建，降低 CI 复杂度。
4. **类型优先**：先落地 `shared/api/endpoints.ts` 与各契约类型，让三端类型同步。

---

## 二、目标目录结构（shared 扩容）

```
shared/
├── constants/            # ✓ 已完成
├── types/                # ✓ 已完成
├── validators/           # ✓ 已完成
├── api/
│   └── endpoints.ts      # ★ 新增：所有后端端点的 method/path/params/response 契约
│   └── game-scores.ts    # ★ 新增：小游戏得分契约 + gameKey→显示名 + 节流常量
│   └── chat-history.ts   # ★ 新增：AI 会话契约 + 消息 DTO
├── utils/
│   ├── sse-parser.ts     # ★ 新增：SSE 分片解析 + [DONE] 协议
│   ├── game-helpers.ts   # ★ 新增：rand/shuffle/clamp/fmtTime
│   ├── student.ts        # ★ 新增：defaultParentPassword(studentNo)
│   ├── general.ts        # ★ 新增：safeParse
│   └── export-data.ts    # ★ 阶段2：XLSX/DOCX 内容生成（无平台层）
├── schemas/
│   ├── crud.ts           # ★ 阶段1：从 mini crud-schema.js 提升
│   ├── subject-tools.ts  # ★ 阶段1：从 mini subject-schema.js 提升
│   └── quicktool.ts      # ★ 阶段1：从 mini quicktool-schema.js 提升
└── auth/
    └── machine.ts        # ★ 阶段2：鉴权状态机接口
```

---

## 三、阶段 1：直接抽取（零破坏，预估 2-3 天）

> 目标：把纯数据/纯函数从两端复制到 shared，两端改为重新导出。不改任何运行行为。

### 1.1 `shared/api/endpoints.ts`（★ 本次新增部分最先做）
- 抽取范围：本次新增的 `game-scores` 与 `chat-sessions` 两端端点。
- 内容：为每个端点定义 `{ method, path, params, response }` 类型。
  - `POST /game-scores`（body: `{gameKey,gameName?,score,durationSec?}`）
  - `GET /game-scores`、`GET /game-scores/:gameKey`
  - `POST /chat-sessions`、`GET /chat-sessions`、`GET /chat-sessions/:id`、`PATCH /chat-sessions/:id/messages`、`PATCH /chat-sessions/:id/pin`、`DELETE /chat-sessions/:id`
- 两端 `web-app/src/api/games.ts`、`chat.ts` 与 `mini-program/src/common/game-score.js`、`chat-history.js` 从 shared 导入类型；请求实现各自保留。
- 同时把"gameKey→显示名"映射抽到 `shared/api/game-scores.ts`，两端复用（现两端各维护一份，易漂移）。
- 验证：`tsc` 通过 + 两端页面功能回归。

### 1.2 `shared/utils/sse-parser.ts`
- 抽取 Web `AiChat.vue` 内联 SSE 解析 + 小程序 `request.js` `streamChat` 的 `feed()`。
- 提供 `parseSSE(chunk, onDelta)` + `[DONE]` 终止 + 错误处理。两端各自传 transport adapter。
- 验证：AI 流式对话两端可用。

### 1.3 `shared/utils/game-helpers.ts`、`student.ts`、`general.ts`
- 从 `mini-program/src/common/game.js` 抽 `rand/shuffle/clamp/fmtTime`。
- 从两端内联抽 `defaultParentPassword(studentNo)`、`safeParse`。
- 验证：相关游戏/表单可用。

### 1.4 `shared/constants/security.ts`
- 抽 `SESSION_INVALID_PATTERNS`（401 判断正则，两端相同）。
- 验证：两端 401 降级逻辑不变。

**阶段 1 验收**：`shared/` 新增 ~600-800 行纯逻辑；两端原文件多数改为 5-10 行重新导出；零运行行为变化。

---

## 四、阶段 2：Adapter 模式（中风险，预估 3-5 天）

> 目标：收敛鉴权、请求契约、导出内容生成、图片压缩策略。

### 2.1 `shared/auth/machine.ts`
- 定义 `AuthState` / `AuthActions` / `FeatureProfile` 类型 + 纯函数 reducer（login/logout/setAuth/applyFeatureProfile）。
- Web `stores/auth.ts` 用 Pinia 实现 adapter；小程序 `common/store.js` 用 reactive 实现 adapter。
- 收益：消除两端鉴权/双角色切换逻辑漂移（本次 g3 已验证两端切换逻辑，抽取后一处维护）。

### 2.2 导出工具内容生成层
- `shared/utils/export-data.ts`：`buildXlsxBuffer` / `buildDocxBuffer`（纯数据→内容）。
- Web `utils/download.ts` 与小程序 `common/exporter.js` 各自保留存盘/打开 adapter。

### 2.3 图片压缩策略
- `shared/utils/image-spec.ts`：压缩参数（maxWidth/quality）+ 纯函数裁剪计算。
- 两端各自调用平台原生 Canvas/Image API。

**阶段 2 验收**：`shared/` 累计新增 ~1400 行；两端 adapter 薄；`tsc` + 回归通过。

---

## 五、阶段 3：Schema 驱动重构（高风险、大收益，预估 2-3 周）

> 目标：用一份 schema 驱动两端页面，减少手工 CRUD / AI 工具 view。

1. **Schema 提升**：`crud-schema.js` → `shared/schemas/crud.ts`、`subject-schema.js` → `subject-tools.ts`、`quicktool-schema.js` → `quicktool.ts`。
2. **Web 通用渲染器**：Web 新增 schema-driven 的 `CrudTable` / `CrudForm` / `AiGenerator` 渲染器，替代部分手工 CRUD view。
3. **游戏状态机**：把贪吃蛇/2048/数独等核心状态机抽为 `shared/games/*.ts` 纯函数，两端各自渲染（Canvas vs DOM）。

**阶段 3 验收**：CRUD 手工 view 减少 70-80%，AI 工具减少 60-70%，游戏状态机抽取 40-50%。

---

## 六、分阶段执行顺序与依赖

| 序 | 动作 | 依赖 | 产出 | 预估 |
|---|---|---|---|---|
| 1 | 抽 `shared/api/endpoints.ts`（含 game-scores/chat-history） | 无 | 两端 API 层类型收敛 | 0.5 天 |
| 2 | 抽 `shared/utils/sse-parser.ts` | 无 | 两端 AI 流式统一 | 0.5 天 |
| 3 | 抽 `game-helpers` / `student` / `general` / `security` 常量 | 无 | 通用 util 收敛 | 1 天 |
| 4 | Schema 提升（crud/subject/quicktool → shared/schemas） | 1-3 | Web 可引用的 schema | 1-2 天 |
| 5 | `shared/auth/machine.ts` | 4 | 两端鉴权状态机统一 | 2 天 |
| 6 | 导出内容生成 + 图片压缩 spec | 5 | 无平台内容层 | 1-2 天 |
| 7 | Web schema-driven 渲染器 + 游戏状态机 | 4-6 | 阶段 3 收益 | 1-2 周 |

**首推先做序 1-3**（约 2 天，零风险），即可：统一本次新增的得分/历史两端 API 层、统一 AI 流式解析，为后续阶段打地基。

---

## 七、风险与回滚

- 阶段 1-2 为"组织重构、不改行为"，风险低；每步用 `tsc --noEmit` + 两端页面回归验证。
- 阶段 3 涉及 100+ 文件，需 e2e 回归 + 小程序云托管联调，建议逐步灰度（先 1-2 个 CRUD 模块试点）。
- 回滚：所有步骤均为文件级变更，git 单 commit 可整体回退。

---

*计划基于本次功能补齐后的代码快照（2026-08）。执行时如遇 `shared/` 结构或 endpoints 变更，以本计划第二节目录为唯一事实来源同步更新。*