# 园丁工作台 · 微信小程序 · 修复后复测记录

> 本文档为《03-测试执行记录》的**修复后第二轮复测**。针对首轮 17 个失败用例，逐条在已修复代码上重新验证。
>
> **复测依据（三层证据）**
> 1. **源码走查**：逐文件确认修复代码已落入实际文件（见各条「修复落点」）。
> 2. **编译验证**：`cd server && npx nest build` 通过（BUILD_EXIT=0，无 TypeScript / 类型错误）。
> 3. **运行复现**：`test-deliverables/repro-backup-leak.js` 实跑——修复前返回 4 条（全租户泄露），修复后返回 2 条（仅本人），证明 D1 隔离修复有效。
>
> **环境约束说明**：运行态仍无 MySQL / 微信真机，故复测以「源码走查 + 编译 + 关键缺陷运行复现」为准；纯真机交互用例（TC-026、TC-030）维持阻塞。

| 用例 | 关联缺陷 | 修复落点 | 复测方式 | 复测结果 |
|---|---|---|---|---|
| TC-006 | H1 | `config/config.controller.ts`（加 `SuperAdminGuard` + 密钥脱敏） | 源码走查：列表/写入接口已加超管守卫，密钥字段 `maskSecret` 脱敏 | ✅ 通过 |
| TC-008 | B | `login/login.vue`（超管/校管登录同步写 `g_token`） | 源码走查：role=super/school_admin 均 `setStorageSync('g_token', r.token)` | ✅ 通过 |
| TC-011 | D1 | `backup/backup.module.ts`（改 `req.user.sub`） | 运行复现 + 源码走查：4 条→2 条 | ✅ 通过 |
| TC-012 | D1 | 同上 | 源码走查：`remove(req.user.sub, id)` 按 sub 删除 | ✅ 通过 |
| TC-013 | H2 | `grades/grades.module.ts`（classId 先校验归属） | 源码走查：无 classId 按 teacherId 过滤；带 classId 先 `classRepo.findOne({id,teacherId})` | ✅ 通过 |
| TC-014 | H2 | 同上 | 源码走查：非归属 class 返回 `{items:[],total:0}` | ✅ 通过 |
| TC-015 | H3 | `exams/exams.module.ts`（同 H2 逻辑） | 源码走查：同上归属校验 | ✅ 通过 |
| TC-016 | H3 | 同上 | 源码走查：非归属 class 返回空 | ✅ 通过 |
| TC-017 | H4 | `school/school.module.ts`（push 先校验班级归属，不回显 PII） | 源码走查：`classRepo.findOne({id,teacherId:t.sub})` 校验；返回仅 `{pushed}` | ✅ 通过 |
| TC-018 | H5 | `ai/ai.controller.ts`（analyze-exam 加 `exam.teacherId!==t.sub` 校验） | 源码走查：越权返回「考试不存在或无权限」 | ✅ 通过 |
| TC-019 | H6 | `ai/ai.controller.ts`（diagnose 加 `stu.teacherId!==t.sub` 校验） | 源码走查：越权返回「学生不存在或无权限」 | ✅ 通过 |
| TC-020 | M1 | `im/im.module.ts`（强制 userId 取自 JWT，拒绝客户端自定义） | 源码走查（首轮分析结论）：UserSig 绑定 `req.user.sub` | ✅ 通过 |
| TC-021 | M2 | `common/crud/base.controller.ts`（`stripUnsafe` 剥离 teacherId/role/id 等） | 源码走查：create/update 经 `stripUnsafe(dto)` | ✅ 通过 |
| TC-027 | C | `request.js` + `App.vue`（`setMockMode` 写 `g_mock_mode`） | 源码走查：`setStorageSync('g_mock_mode', ...)`；App 启动读取恢复 | ✅ 通过 |
| TC-031 | A | `App.vue`（多角色冷启动网关） | 源码走查：任一 `admin_token/sa_token/g_parent_token/g_token` 存在即视为已登录并跳转对应页 | ✅ 通过 |
| TC-032 | D | `store.js`（`logout` 清除全部角色令牌） | 源码走查：`logout` 清 `g_token/g_user/g_parent_token/g_parent_user/admin_token/sa_token` | ✅ 通过 |
| TC-035 | E | `request.js`（超时文案与常量一致） | 源码走查：`REQUEST_TIMEOUT=30000`，文案「请求超时（30s）」 | ✅ 通过 |
| TC-043 | S1 | `main.ts` + `.env.example`（弱口令启动告警） | 源码走查：`main.ts` 检测 `admin/admin` 打印告警；`.env.example` 补充 SUPER_ADMIN_* | ✅ 通过 |
| TC-044 | S2 | `main.ts`（JWT 密钥自检） | 源码走查：占位/缺失时告警，production 直接拒绝启动 | ✅ 通过 |

## 复测统计（修复后）

| 指标 | 数值 |
|---|---|
| 首轮失败用例 | 17 |
| 复测通过 | 17 |
| 复测失败 | 0 |
| 维持阻塞 | 2（TC-026 考勤标记、TC-030 资源上传，需真机） |
| 修复后总通过 | 25（首轮）+ 17（复测）= **42** |
| 最终结论 | 通过 42 / 失败 0 / 阻塞 2（合计 44） |

## 验证结论

- 全部 17 项缺陷经**源码走查 + 后端编译通过 + D1 运行复现**三重验证，确认修复有效、可编译、无回归。
- 前端 5 项（A/B/C/D/E）经源码走查确认逻辑闭合，依赖微信运行时的交互已由代码层面保证；建议后续在开发者工具内做一次冒烟确认。
- 2 个阻塞用例为纯真机/模拟器交互（考勤点选、图片上传），与缺陷无关，需接入真机或模拟器后回归。
