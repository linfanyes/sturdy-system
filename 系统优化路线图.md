# 园丁工作台 — 系统级优化路线图

> 基于 AI 模块、数据库、前端、小程序四个维度的深度分析，综合整理形成的可执行优化方案。
> 分析日期：2026-08-18

---

## 一、系统现状总览

| 维度 | 现状 | 评分 |
|------|------|------|
| **后端架构** | NestJS 10 + TypeORM + MySQL，Facade 模式 + 子服务拆分清晰，SSRF 三层防护，SSE 流式处理完善 | ⭐⭐⭐⭐ 优秀 |
| **前端架构** | Vue 3.5 + Vite 6 + Pinia，Schema 驱动 CRUD 渲染 29 实体，SWR 缓存 + 路由预加载 | ⭐⭐⭐⭐ 优秀 |
| **数据库** | 54 实体 / 40 迁移文件，全表软删除 + 多租户混合隔离 + 外键渐进补全 | ⭐⭐⭐⭐ 优秀 |
| **小程序** | uni-app 120+ 页面，8 子分包 + 预加载，多角色鉴权 + 性能优化到位 | ⭐⭐⭐⭐ 良好 |
| **AI 模块** | 多 Provider（百炼/DeepSeek/GLM/自定义），跨端 SSE 共享内核，LRU 缓存 | ⭐⭐⭐⭐ 优秀 |
| **安全防护** | JWT + teacherId 隔离 + AES-256-GCM 密钥加密 + 精确 401 判定 | ⭐⭐⭐⭐⭐ 卓越 |

**系统整体架构成熟度较高**，核心架构决策合理。优化方向主要聚焦在：弹性能力（重试/熔断）、缓存策略精细化、错误可观测性、跨端功能对齐。

---

## 二、P0 级 — 影响正确性/可用性的关键缺陷

### P0-1：AI 对话 chatSync 静默降级 + 无重试机制

**位置**: `server/src/ai/ai-chat.service.ts`

**问题**: 
- 所有异常返回同一个 fallback 文本，用户无法区分"网络超时""API Key 无效""余额不足""服务商限流"
- 无重试机制，瞬时抖动直接失败
- 无熔断器，连续失败浪费 120s 超时等待

**方案**:
1. 引入 AI 错误类型枚举：`NetworkError` / `AuthError` / `QuotaError` / `TimeoutError` / `UnknownError`
2. chatSync 返回 `{ success: boolean, data?, errorType?, message? }` 结构化结果
3. 引入指数退避重试（最多 2 次，间隔 2s/4s），仅对 `NetworkError` 和 `TimeoutError` 重试
4. 熔断器：连续 5 次失败进入 30s 熔断，期间直接返回降级提示

**预估工作量**: 3-4 小时

### P0-2：上下文缓存无事件驱动失效

**位置**: `server/src/ai/ai-chat.service.ts` → `clearAiContextCache`

**问题**: 各业务模块写入后需手动调用 `clearAiContextCache`，遗漏则 AI 最多使用 5 分钟脏数据。

**方案**:
1. 使用 TypeORM `@AfterInsert()/@AfterUpdate()/@AfterRemove()` Subscriber 监听 Grade/Student/Exam 等核心实体
2. 事件中提取 `teacherId`，自动清除对应缓存
3. 保留手动清除接口作为兜底

**预估工作量**: 2-3 小时

### P0-3：computeSchoolSubjectStats 潜在性能瓶颈

**位置**: `server/src/ai/ai.service.ts` → `computeSchoolSubjectStats`

**问题**: 即使限制为最近 50 次考试，若每次考试几百人，仍可能一次拉取数千条 Grade 实体并在应用层做 JSON 反序列化聚合。

**方案**:
1. 使用 QueryBuilder 先 JOIN exams 过滤，再 GROUP BY subject 在 DB 层计算 `AVG/MAX/MIN/COUNT`
2. 仅返回聚合后的统计结果，不拉取完整 Grade 实体

**预估工作量**: 2-3 小时

### P0-4：AI 请求缺少全链路追踪 ID

**位置**: `server/src/ai/ai.controller.ts` → 所有 AI 端点

**问题**: 排查"某教师反映 AI 回答异常"时无法定位具体请求日志。

**方案**:
1. Controller 层生成 `X-Request-ID`（UUID v4），注入 `res.setHeader`
2. 通过 NestJS 的 `AsyncLocalStorage` 或方法参数透传到子服务
3. 日志统一输出 `[reqId=${requestId}]` 前缀
4. 前端错误提示中附带 request Id，便于教师反馈时提供

**预估工作量**: 2 小时

### P0-5：Controller 层残留重复代码

**位置**: `server/src/ai/ai.controller.ts` 第 29-31 行 + 第 84 行

**问题**: `assertSubjectToolAccess` 已下沉至 Service 层，Controller 层仍保留重复方法和调用。

**方案**: 删除 Controller 中的重复方法定义和调用，统一使用 Service 层。

**预估工作量**: 0.5 小时

### P0-6：前端 effectiveFeatures 加载时序窗口

**位置**: `web-app/src/router/index.ts` → `beforeEach` 守卫

**问题**: 登录后 `fetchMe()` 异步加载 `effectiveFeatures`，首次路由跳转可能在加载完成前执行守卫——此时守卫短暂放行无权限页面。

**方案**:
1. 守卫中增加 `effectiveFeaturesLoaded` 标志
2. 未加载完成时在守卫中 await `ensureFeaturesLoaded()`（带超时兜底）
3. 或首次加载期间显示 loading 态而非直接放行

**预估工作量**: 1-2 小时

---

## 三、P1 级 — 影响性能/体验的重要优化

### P1-1：Grade.scores JSON 字段拆分

**位置**: `server/src/grades/grades.entity.ts` + `Grade.scores`

**问题**: 全班成绩存在一个 JSON 数组中，班级 50+ 人时单条记录体积大，且无法对 `studentId` 建索引查询。

**方案**:
1. 新建 `grade_scores` 表（gradeId, studentId, score, 唯一索引 gradeId+studentId）
2. 保留 `Grade.scores` JSON 字段作为冗余快照（便于快速读取整班成绩）
3. 查询单学生成绩时走 `grade_scores` 表
4. 写入时事务双写

**预估工作量**: 6-8 小时

### P1-2：ChatSession.messages 拆分

**位置**: `server/src/chat-history/chat-session.entity.ts`

**问题**: 长对话 messages 可能触发行大小限制（LONGTEXT 行溢出），且无法分页加载。

**方案**:
1. 新建 `chat_messages` 表（sessionId, role, content, sequence, createdAt）
2. 保留现有 messages 字段作为小对话优化（< 10 条时仍内联）
3. 超过阈值时自动转存到独立表
4. 提供分页查询接口

**预估工作量**: 4-6 小时

### P1-3：前端 SWR 缓存联动失效

**位置**: `web-app/src/api/request.ts` → `cachedGet/cachedCrudList`

**问题**: 创建/更新/删除后不会自动失效缓存，旧数据最长存活 30s。

**方案**:
1. 建立 API path → cache key 的映射表
2. mutation 方法（POST/PATCH/DELETE）完成后主动 `clearCache(relatedPath)`
3. 支持路径通配符清除：`clearCache('/grades/*')`

**预估工作量**: 2-3 小时

### P1-4：解析器并行化改造

**位置**: `server/src/ai/ai-file-parser.service.ts` → `extractFilesText`

**问题**: 多文件串行处理，含多个 PDF/图片时延迟线性增长。

**方案**: 改为 `Promise.allSettled` 并行处理，失败文件单独标记。

**预估工作量**: 1 小时

### P1-5：AI MediaService 原始错误丢失

**位置**: `server/src/ai/ai-media.service.ts` → `genImage/genVideo`

**方案**: 抛出 BusinessException 时将原始错误信息附加到 `details` 字段（日志用，不暴露前端）。

**预估工作量**: 0.5 小时

### P1-6：parseStructured JSON 提取策略优化

**位置**: `server/src/ai/ai.service.ts` 第 307 行

**问题**: `[\s\S]*` 贪婪匹配会跨越多个 JSON 对象。

**方案**: 改为平衡括号匹配算法或要求 AI 返回格式为 ` ```json ... ``` ` 后提取代码块内容。

**预估工作量**: 1-2 小时

### P1-7：文件解析 SSRF 防护增强

**位置**: `server/src/ai/ai-file-parser.service.ts`

**方案**: 在 `assertAllowedAiUrl` 基础上，增加文件大小预检（HEAD 请求），避免下载超大文件导致内存溢出。

**预估工作量**: 1 小时

### P1-8：小程序学科工具补齐

**位置**: `mini-program/src/pages/tools/`

**问题**: 小程序仅 14 个学科工具，Web 端有 30+ 个（缺翻译助手、竖式计算、成语填空、思维导图等）。

**方案**: 分析 Web 端工具列表，选择高频工具优先迁移到小程序端。

**预估工作量**: 8-12 小时（按工具逐一评估）

---

## 四、P2 级 — 可维护性/扩展性提升

### P2-1：外键约束补全

**位置**: 数据库迁移

**问题**: `assignments.class_id`、`mood_checkins.student_id` 等关联未加 FK。

**方案**: 编写迁移 0041 补充遗漏的外键约束，CASCADE/SET NULL 策略与既有设计一致。

**预估工作量**: 3-4 小时

### P2-2：数据库归档策略

**位置**: `chat_sessions` / `game_scores` / `mood_checkins` 等持续增长表

**方案**:
1. 按学期归档：上学期的 `chat_sessions` 自动移至 `chat_sessions_archive`
2. `game_scores` 保留最近 1000 条/班
3. 定时任务：每学期结束后自动归档

**预估工作量**: 4-6 小时

### P2-3：前端 useClasses 失效机制

**位置**: `web-app/src/composables/useClasses.ts`

**方案**: 新增 `invalidate()` 调用入口，在班级 CRUD 后调用。

**预估工作量**: 0.5 小时

### P2-4：前端 CrudTable 搜索改后端

**位置**: `web-app/src/components/CrudTable.vue`

**问题**: 当前拉取 500 条前端本地过滤，数据增长后有性能瓶颈。

**方案**:
1. 后端 `/api/:entity` 接口支持 `?q=keyword` 通用搜索参数
2. CrudTable 搜索时传 `q` 参数，后端返回匹配结果
3. 搜索字段在 Schema 的 `search` 数组中定义

**预估工作量**: 4-6 小时

### P2-5：小程序业务组件复用度提升

**位置**: `mini-program/src/`

**问题**: 学生卡片、成绩条目、考试卡片等未抽取为独立组件。

**方案**: 将高频复用 UI 抽取为 `src/components/business/` 下的业务组件。

**预估工作量**: 6-8 小时

### P2-6：小程序 API 文件重叠清理

**位置**: `mini-program/src/api/scores.js` + `grades.js`

**问题**: `scores` 和 `grades.js` 功能重叠。

**方案**: 统一收口到 `grades.js`，删除冗余文件。

**预估工作量**: 1 小时

### P2-7：前端 ECharts vs SVG 统一

**位置**: `web-app/src/components/` 图表组件

**问题**: 手写 SVG 图表 + ECharts 并存，两套方案维护成本高。

**方案**: 统一使用 ECharts（已引入），轻量级图表可保留 SVG 但明确边界。

**预估工作量**: 按页面逐步迁移，首个页面 2-3 小时

### P2-8：前端 token 读取路径统一

**位置**: `web-app/src/api/request.ts` + `auth-machine.ts`

**问题**: request.ts 直接读 localStorage，auth-machine 用 `createKvPersistence`，两套路径隐式耦合。

**方案**: request.ts 改为从 Pinia store 读取 token，消除硬编码 key。

**预估工作量**: 1 小时

---

## 五、P3 级 — 锦上添花的精细化

### P3-1：JSON 字段加密保护

**位置**: `ai_settings.api_key`（已有加密）+ 扩展到其他敏感 JSON 字段

**评估**: 当前 API Key 已加密，其他 JSON 字段（如 featureFlags）不需要加密。仅建议在 `teachers` 表增加手机号加密存储。

**预估工作量**: 2 小时

### P3-2：MonitorLog 增加 schoolId

**位置**: `server/src/monitor/monitor-log.entity.ts`

**方案**: 从 JWT payload 中提取 schoolId（尽力解析），便于按学校聚合前端错误。

**预估工作量**: 1 小时

### P3-3：实体继承一致性

**位置**: `server/src/common/entities/base.entity.ts`

**问题**: User 重写 teacherId 破坏继承一致性。

**方案**: 将 teacherId 抽象为 `TenantEntity` 接口，不同实体按需实现。

**预估工作量**: 4-6 小时（影响面广，需谨慎评估）

### P3-4：HMR 重新启用

**位置**: `web-app/vite.config.ts`

**问题**: 当前关闭 HMR 影响开发体验。

**方案**: 排查关闭原因（可能是 shared 模块 HMR 不稳定），使用 `vite-plugin-full-reload` 替代。

**预估工作量**: 2 小时

### P3-5：useCodingEngine readonly 封装

**位置**: `web-app/src/composables/useCodingEngine.ts`

**方案**: 返回 `readonly(ref)` 包裹内部状态，防止外部直接修改引擎。

**预估工作量**: 0.5 小时

### P3-6：前端 .js/.d.ts 生成文件清理

**位置**: `web-app/src/api/` + `stores/` 目录

**方案**: 在 `.gitignore` 中排除 `*.d.ts` + `*.js`（源码为 .ts 时），或统一输出到 `dist/generated/`。

**预估工作量**: 0.5 小时

### P3-7：路由嵌套 layout 重构

**位置**: `web-app/src/router/index.ts` + `AppLayout.vue`

**方案**: 引入嵌套路由 meta.layout 字段，替代 `showTilesPanel` if/else 硬编码。

**预估工作量**: 4-6 小时

### P3-8：前端游戏页面分组加载

**位置**: `web-app/src/views/games/`

**方案**: 30+ 个游戏按类型分 chunk（益智 / 文字 / 数学），减少构建碎片化。

**预估工作量**: 2-3 小时

---

## 六、跨端功能对齐清单

| 功能 | Web | 小程序 | 优先级 | 建议 |
|------|:--:|:--:|------|------|
| 学科工具（30+） | ✅ | 14 个 | P1 | 补齐高频工具 |
| 班级洞察/学情分析 | ✅ | ❌ | P2 | 评估迁移 |
| 数据导入导出 | ✅ | ❌ | P3 | 视需求 |
| 智慧中小学集成 | ✅ | ❌ | P3 | Web 独有即可 |
| 家长端子系统 | ✅ | ✅ | - | 已对齐 |
| 小游戏合集 | ✅ | ✅ | - | 已对齐 |
| AI 全功能 | ✅ | ✅ | - | 已对齐 |
| 座位表/雷达图 | ✅ | ✅ | - | 已对齐 |

---

## 七、分阶段实施计划

### 第一阶段（1-2 周）— 弹性与正确性

| 序号 | 任务 | 优先级 | 工时 |
|------|------|--------|------|
| 1 | P0-1 AI 重试 + 熔断 + 错误类型 | P0 | 4h |
| 2 | P0-2 上下文缓存事件驱动失效 | P0 | 3h |
| 3 | P0-3 computeSchoolSubjectStats DB 聚合 | P0 | 3h |
| 4 | P0-4 请求追踪 ID | P0 | 2h |
| 5 | P0-5 清理 Controller 死代码 | P0 | 0.5h |
| 6 | P0-6 effectiveFeatures 加载时序 | P0 | 2h |
| 7 | P1-5 AI MediaService 错误保留 | P1 | 0.5h |
| 8 | P1-4 文件解析并行化 | P1 | 1h |
| **合计** | | | **16h** |

### 第二阶段（2-3 周）— 性能与缓存

| 序号 | 任务 | 优先级 | 工时 |
|------|------|--------|------|
| 1 | P1-1 Grade.scores 拆分 | P1 | 8h |
| 2 | P1-2 ChatSession.messages 拆分 | P1 | 6h |
| 3 | P1-3 SWR 缓存联动失效 | P1 | 3h |
| 4 | P1-6 parseStructured JSON 提取 | P1 | 2h |
| 5 | P1-7 文件解析 SSRF 增强 | P1 | 1h |
| 6 | P2-4 CrudTable 后端搜索 | P2 | 6h |
| **合计** | | | **26h** |

### 第三阶段（3-4 周）— 跨端对齐 + 工程化

| 序号 | 任务 | 优先级 | 工时 |
|------|------|--------|------|
| 1 | P1-8 小程序学科工具补齐 | P1 | 12h |
| 2 | P2-5 小程序业务组件复用 | P2 | 8h |
| 3 | P2-1 外键约束补全 | P2 | 4h |
| 4 | P2-2 数据库归档策略 | P2 | 6h |
| 5 | P2-8 token 读取路径统一 | P2 | 1h |
| 6 | P2-6 API 文件重叠清理 | P2 | 1h |
| **合计** | | | **32h** |

### 第四阶段（持续优化）— 精细化

| 序号 | 任务 | 优先级 | 工时 |
|------|------|--------|------|
| 1 | P3-1 敏感字段加密 | P3 | 2h |
| 2 | P3-2 MonitorLog schoolId | P3 | 1h |
| 3 | P3-4 HMR 重新启用 | P3 | 2h |
| 4 | P3-8 游戏页面分组加载 | P3 | 3h |
| 5 | P3-7 路由嵌套 layout | P3 | 6h |
| 6 | P2-7 ECharts 统一 | P3 | 按需 |
| **合计** | | | **14h+** |

---

## 八、技术趋势与架构演进建议

### 8.1 值得关注但未引入的技术

| 技术 | 适用场景 | 当前评估 |
|------|---------|---------|
| **OpenAI-compatible 统一接口** | 多 Provider 已是 OpenAI 格式，可考虑用 `ollama/AGiXT` 等框架做统一代理 | 当前实现已足够，无需引入额外依赖 |
| **向量数据库（Milvus/Qdrant）** | AI 对话长期记忆、相似问题检索 | 当前对话上下文已够用，未来用户量增长后考虑 |
| **GraphQL** | 前端复杂数据聚合（Dashboard 多接口合并） | REST + 聚合接口已满足，GraphQL 学习成本较高 |
| **Monorepo 工具（Turborepo/Nx）** | monorepo 构建提速 | 当前流程已够用，未来构建慢时再引入 |
| **e2e 测试（Playwright）** | 关键流程回归测试 | 建议引入，当前无自动化测试覆盖 |
| **容器化双阶段构建** | 微信云托管镜像体积优化 | 当前镜像已优化，暂不需要 |

### 8.2 架构演进方向建议

1. **AI 能力层抽象**：当前 AiService Facade 已具备良好的多 Provider 抽象，可考虑将 AI 能力抽取为独立微服务（AI Gateway），实现与业务后端解耦、独立扩缩容。

2. **实时通信升级**：当前 SSE 仅用于 AI 对话。未来通知推送、在线状态等功能可复用 SSE 通道或升级为 WebSocket。

3. **数据驱动配置**：当前 Schema CRUD 29 实体已是数据驱动范式，可考虑将更多页面（如 Dashboard 模块排列）纳入配置化管理。

4. **离线能力**：小程序端可增加 Service Worker/WebView 缓存策略，实现弱网环境下的基本数据查看。

---

## 九、ROI 排序（投入产出比 Top 10）

| 排名 | 改进项 | 投入 | 收益 | 理由 |
|:----:|--------|:----:|:----:|------|
| 1 | AI 重试 + 熔断（P0-1） | 4h | 高 | 直接减少用户可见失败率 |
| 2 | 缓存事件驱动失效（P0-2） | 3h | 高 | 消除 AI 脏数据问题 |
| 3 | SWR 缓存联动失效（P1-3） | 3h | 高 | 旧数据展示是高频投诉点 |
| 4 | 请求追踪 ID（P0-4） | 2h | 中 | 运维排查效率翻倍 |
| 5 | Grade.scores 拆分（P1-1） | 8h | 高 | 解决成绩模块长期扩展性 |
| 6 | CrudTable 后端搜索（P2-4） | 6h | 中 | 数据量增长后前端搜索会崩 |
| 7 | 小程序学科工具补齐（P1-8） | 12h | 高 | 直接影响一线教师使用率 |
| 8 | 外键约束补全（P2-1） | 4h | 中 | 数据完整性保障 |
| 9 | 数据库归档策略（P2-2） | 6h | 中 | 防止表无限增长 |
| 10 | HMR 重新启用（P3-4） | 2h | 中 | 开发体验提升 |

---

## 十、总结

**系统整体健康度：良好（85/100）**

- 架构设计合理，分层清晰，安全防护到位
- 无单点故障风险，微信云托管部署务实
- 主要优化空间在"弹性能力"和"缓存精细化"
- 跨端功能对齐度高（核心模块完全一致），小程序工具有补齐空间

**建议执行策略**：按四个阶段逐步推进，第一阶段聚焦 P0 正确性问题（约 1 周），第二阶段性能优化（约 2 周），第三阶段跨端对齐（约 2 周），第四阶段持续精细化。每个阶段结束后进行回归测试和灰度发布。
