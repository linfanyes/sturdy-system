# Web 端 / 小程序端复用可行性分析报告

> 分析范围：`work-system` monorepo（K12 教师管理系统）  
> 后端：NestJS · Web：Vue 3 + Vite + Pinia + Tailwind · 小程序：uni-app（Vue 3 编译到微信小程序 + H5）  
> 分析日期：2026-03-17 · 方法：静态代码阅读 + 行数统计 + 模式对比

---

## 一、现状：已复用的部分

| 模块 | 共享方式 | 位置（shared） | Web 导入路径 | 小程序导入路径 | 状态 |
|---|---|---|---|---|---|
| **常量（学科/角色/年级/功能包/主题色/字体等）** | star-export + 路径别名 | `shared/constants/index.ts`（233 行） | `@gardener/shared/constants` | `@gardener/shared/constants` | **已共享** |
| **TypeScript 类型（User/Teacher/Student/Class/ApiResponse 等）** | star-export + 路径别名 | `shared/types/index.ts`（124 行） | `@gardener/shared/types` | `@gardener/shared/types` | **已共享** |
| **校验器（isPhone/validateClassName/hasFeature/parseClassName 等）** | 纯函数 + 路径别名 | `shared/validators/index.ts`（381 行） | `@gardener/shared/validators` | `@gardener/shared/validators` | **已共享** |
| **功能包解析（normalizeLevel/computeEffective/isBlockedBySchool）** | 纯函数 + 路径别名 | `shared/validators/index.ts` L342-382 | 重新导出自 shared | `feature.js` 重新导出自 shared | **已共享** |

### 已共享部分小结

- `shared/` 模块共 **738 行**，通过 `tsconfig paths` 别名 `@gardener/shared/*` 直接引用源码（零发布流程，方案 C）。
- Web 端的 `validators.ts`（19 行）、`subjects.ts`（5 行）、`features.ts`（12 行）均为**空壳重新导出**，实际逻辑全在 shared。
- 小程序端的 `validators.js`（78 行）、`feature.js`（64 行）同样是**重新导出层**，加少量平台扩展（`isEmail`、`inRange`、`isInt`）。
- **关键成就**：学科、角色、功能包 key、正则、校验函数已在两端完全对齐，消除了三份实现漂移的风险。

### 未共享但已高度对齐的部分

| 模块 | Web 位置 | 小程序位置 | 说明 |
|---|---|---|---|
| 鉴权 store / 状态管理 | `web-app/src/stores/auth.ts`（124 行，Pinia） | `mini-program/src/common/store.js`（362 行，reactive） | 登录态/主题/家长双角色切换逻辑高度同名，但实现机制不同（Pinia vs reactive）。**结构可共享，实现需适配**。 |
| 请求层 | `web-app/src/api/request.ts`（98 行，axios） | `mini-program/src/common/request.js`（371 行，wx.cloud.callContainer） | 鉴权注入、401 降级、超时策略逻辑**相同**，但底层 transport 完全不同。**接口契约可共享，实现不可共用**。 |

---

## 二、可共用清单（高 ROI）

> 按**预估节省代码量**降序排列。"节省代码量"= 两端重复部分中可被一份共享实现替代的行数（估算）。

| # | 模块 | Web 位置（行数） | 小程序位置（行数） | 复用方式 | 预估节省代码量 | 理由 |
|---|---|---|---|---|---|---|
| 1 | **CRUD Schema 配置** | 无独立文件（内联在各 view） | `common/crud-schema.js`（458 行） | 抽到 `shared/schemas/crud.js`（JSON 纯数据） | **~400 行**（小程序不再维护；Web 新建 CRUD 配置时直接引用） | 纯数据声明（字段类型/标签/搜索字段），无平台依赖；Web 端完全缺失此层。 |
| 2 | **Subject Schema（学科工具配置）** | 无独立文件（AI 生成内联在各 view） | `common/subject-schema.js`（276 行，14+ 学科） | 抽到 `shared/schemas/subject-tools.js` | **~250 行** | prompt 模板 + 表单字段定义，纯数据；Web 的 `teacher-prompts.ts`（77 行）只覆盖部分场景，平台差异为零。 |
| 3 | **QuickTool Schema** | 无独立文件 | `common/quicktool-schema.js`（217 行） | 抽到 `shared/schemas/quicktool.js` | **~200 行** | 同上，纯配置数据。 |
| 4 | **鉴权 store 状态机接口** | `stores/auth.ts`（124 行） | `common/store.js`（362 行） | 抽到 `shared/` 一个 `auth-machine.ts`（状态 + 动作 + 计算属性的纯函数接口），两端各自用 Pinia/reactive 实现 adapter | **~200-300 行**（消除逻辑漂移） | 两端的 login/logout/setAuth/applyFeatureProfile 函数签名已接近，差异仅在存储层（localStorage vs uni.setStorageSync）。用 shared 定义"做什么"，两端实现"怎么存"。 |
| 5 | **AI 流式对话解析器** | `api/teacher.ts` 中 `aiChatStream`（L343-388，~45 行） + `views/ai/AiChat.vue`（~80 行 in-component） | `common/request.js` 中 `streamChat`（L199-327，~128 行） | 抽到 `shared/utils/sse-parser.ts`（通用 SSE 分片解析 + `[DONE]` 终止协议） | **~100 行** | 两端的 SSE 解析逻辑（data: 行提取、JSON.parse delta、[DONE] 终止、错误处理）**完全相同**，唯一差异是 transport（fetch GETReader vs wx chunk adapter）。 |
| 6 | **导出工具（XLSX/DOCX）** | `utils/download.ts`（67 行，Blob + a 标签） | `common/exporter.js`（104 行，uni FileSystemManager） | 抽到 `shared/utils/export-data.ts`（纯数据→文件内容生成的无平台层：`buildXlsxBuffer`、`buildDocxBuffer`），两端各自实现"存盘+打开"adapter | **~60-80 行** | 文件结构生成（SheetJS 调用 / docx 调用）是重复的；存盘方式不同。 |
| 7 | **图片压缩策略参数 + base64 编码** | `composables/usePhotoUpload.ts`（71 行，Canvas + Image） | `common/image.js`（96 行，OffscreenCanvas + uni API） | 抽到 `shared/utils/image-compress-spec.ts`（参数接口 + 压缩结果类型 + 默认值常量），两端各自实现 | **~50 行**（类型 + 常量） | 目前 Web 的 `compressImage` 与小程序的 `compressImage` API 设计几乎一致（maxWidth=1280, quality=0.7/80），但参数硬编码位置不同。 |
| 8 | **功能包访问守卫逻辑** | `router/index.ts`（`beforeEach` feature 检查） | `common/feature.js`（`hasFeature` + `route-guard.js` canAccess） | 已在 shared 中实现 `hasFeature`/`computeEffective`；当前 `route-guard.js` 和 `router/index.ts` 仍有部分内联判断可进一步收敛 | **~30-50 行** | `feature.js` 的 `hasFunction` 与 `router beforeEach` 的 feature 检查语义完全一致。 |
| 9 | **store 主题/外观/字体管理** | 无独立模块 | `common/store.js` 中 `theme`/`applyAppearance`/`applyColorScheme`/`applyFontSize`（~80 行） | 抽到 `shared/theme-manager.ts`（状态定义 + 切换逻辑），小程序端保留 setTabBarStyle 等平台调用 | **~50-70 行** | SCHEMES、FONT_SIZES 已在 shared 中；store 中的 cycleColorScheme/setFontSize/setColorScheme 是纯逻辑，可直接共享。 |
| 10 | **游戏公共 helper（rand/shuffle/clamp/fmtTime/useGame）** | 各 game view 内联重复 | `common/game.js`（159 行，已抽取） | 抽到 `shared/utils/game-helpers.ts` | **~80-120 行** | `rand/shuffle/clamp/fmtTime` 是纯函数。Web 端各 game 也在重复这些函数，目前只在小程序抽了。 |
| 11 | **通用 util（safeParse）** | 部分 view 内联 | `common/util.js`（17 行） | 抽到 `shared/utils/general.ts` | **~15 行** | `safeParse` 等平台无关函数。 |
| 12 | **学生表单默认密码计算（学号后 6 位）** | `views/teacher/Students.vue`（`defaultPwd` 内联） | `pages/students/students.vue`（`defaultPwd` 内联） | 抽到 `shared/utils/student.ts` `defaultParentPassword(studentNo)` | **~10 行** | 两端实现完全相同的内联函数。 |
| 13 | **401 判断正则** | `api/request.ts` L67 | `common/request.js` L119 | 已在 shared 可提为常量 `SESSION_INVALID_PATTERNS` | **~5 行** | `/登录已过期\|未登录\|缺少令牌\|账号已禁用\|登录已关闭\|账号已被禁用/` 完全相同。 |

### 可复用总量估算

| 类别 | 可节省行数（估算） |
|---|---|
| Schema 配置（CRUD + 学科工具 + 快捷工具） | **~850 行** |
| 状态机接口 + 主题管理 | **~250-350 行** |
| SSE 流式解析 + 401 判断 | **~100-110 行** |
| 导出 + 图片压缩 + 游戏 helper + 通用 util | **~200-250 行** |
| **合计可节省** | **~1400-1560 行** |

---

## 三、不可共用清单（受平台差异限制）

| 模块 | Web 位置 | 小程序位置 | 不可共用原因 | 说明 |
|---|---|---|---|---|
| **请求 Transport 实现** | `api/request.ts`（axios） | `common/request.js`（wx.cloud.callContainer） | 底层 API 完全不同（XMLHttpRequest vs 微信云托管私有链路） | 接口契约（api 方法的签名）可共享，transport 不可。 |
| **登录/鉴权入口** | `views/Login.vue` + `api/auth.ts`（用户名/密码 + JWT） | `pages/login/login.vue`（学号/密码 + 微信 code + JWT） | 小程序需微信 `code` + `wx.login`，Web 只需用户名/密码 | 登录流程差异大。 |
| **UI 组件** | `components/`（Modal/CrudTable/EmptyState 等，Tailwind 类名） | `components/`（EmptyState/Skeleton，uni-view 标签 + WXSS） | 渲染层不同（DOM vs 小程序自定义组件模型 + 无 CSS 原生框） | 组件产品设计可共享，实现不可共用。 |
| **导航 / TabBar** | `router/index.ts`（Vue Router + hash 模式） | `pages.json` + `manifest.json`（微信小程序原生 tabBar + 分包） | 路由机制完全不同 | meta.feature 对功能包守卫逻辑**可共享**，结构不可。 |
| **导出文件到本地** | `utils/download.ts`（Blob + a[download] + URL.createObjectURL） | `common/exporter.js`（wx.env.USER_DATA_PATH + fs.writeFile + wx.openDocument） | DOM API vs 小程序沙箱文件系统 | 文件内容生成可共用（见可共用清单 #6），保存/打开不可。 |
| **图片压缩实现** | `composables/usePhotoUpload.ts`（Canvas + Image + toDataURL） | `common/image.js`（OffscreenCanvas + uni.getImageInfo + uni.canvasToTempFilePath） | 浏览器 Canvas API vs 小程序 Canvas API | 压缩策略参数可共享，API 调用不可。 |
| **小游戏渲染层** | `views/games/*.vue`（DOM/CSS animation + requestAnimationFrame） | `pages/games/*.vue`（Canvas 2D + 微信触摸事件 + uni 页面生命周期） | 渲染模型与事件模型完全不同 | 游戏**玩法逻辑**（棋盘状态机/计分规则）可共享，**渲染 + 输入**不可。 |
| **微信支付 / IM 实时通信** | 未启用 | 云托管私有链路 + 未来可能 `wx.subscribeMessage` | 平台独占能力 | 仅在小程序可用。 |
| **分包 / 预加载策略** | Vite 代码分割（dynamic import） | `pages.json` subPackages + 微信小程序 2M 主包限制 | 打包机制不同 | 共享代码需同时满足 Web chunk size 和微信小程序分包体积限制。 |

---

## 四、改造路径建议（分阶段）

### 阶段 1：直接抽到 `shared/`（低风险、零破坏，预估 2-3 天）

**动作**：把纯数据/纯函数从两端复制到 `shared/`，两端改为重新导出。

| 抽取项 | 来源 | 方式 | 预计节省 |
|---|---|---|---|
| SSE 流式解析器 | `api/teacher.ts` + `common/request.js` | `shared/utils/sse-parser.ts` | ~100 行 |
| 通用游戏 helper | `common/game.js` 的纯函数部分 | `shared/utils/game-helpers.ts` | ~80 行 |
| CRUD / SubjectTool / QuickTool Schema | 小程序端 | `shared/schemas/*.js` | ~850 行 |
| safeParse + defaultParentPassword | 两端内联 | `shared/utils/general.ts` + `shared/utils/student.ts` | ~25 行 |
| SESSION_INVALID_PATTERNS | `request.ts` + `request.js` | `shared/constants/security.ts` | ~5 行 |

**评估**：
- 改动量：~1060 行新增到 shared，两端原文件改为 ~5-10 行重新导出 + adapter。
- 风险：**零**（shared 源码通过 paths 别名直接引用，无需构建发布流程）。
- **节省约 30-35%** 的可共享代码重复。

### 阶段 2：条件编译 / Adapter 模式（中风险，预估 3-5 天）

**动作**：收敛鉴权 store、请求接口契约、导出工具的内容生成层。

| 抽取项 | 共享方式 | 平台 adapter |
|---|---|---|
| 鉴权状态机接口 | `shared/auth-machine.ts` 定义状态类型 + 动作签名 | Web: Pinia 实现；小程序：reactive 实现 |
| 请求层接口契约 | `shared/api-contracts.ts` 定义每个 endpoint 的参数/返回类型 | 请求实现各自保留（axios vs callContainer） |
| 导出工具内容生成 | `shared/utils/export-data.ts`（buildXlsxBuffer / buildDocxBuffer） | Web: Blob + a 标签；小程序: uni 文件系统 |
| 图片压缩策略 | `shared/utils/image-compress-spec.ts`（参数 + 纯函数 crop 计算） | 各自调用本平台原生 API |

**评估**：
- 改动量：~600-800 行新增到 shared + 各自端 ~100-200 行 adapter 改动。
- 风险：**低**（不改变运行行为，只改变组织方式；shared 模块编译即可验证）。
- **额外节省约 20-25%** 的重复逻辑（累计 50-55%）。

### 阶段 3：重构为一端为主的 composition / 小程序优先（高风险、大收益，预估 2-3 周）

**动作**：
1. **小程序优先 CRUD 页面生成**：把 `crud-schema.js` + `subject-schema.js` + `quicktool-schema.js` 合为统一的 Schema 驱动引擎。Web 端可以使用同一套 schema 渲染 Table + Form（替代目前的 60+ 手工 CRUD view）。
2. **AI 工具渲染器**：当前 Web 的 60+ tools view 和 mini 的 30+ subject-tools page 都基于相同的 `subject-schema.js` prompt 模板。可以做出一个 schema-driven 的通用"AI 生成器页面"，两端各一个 .vue 文件解决。
3. **通用游戏状态机**：把贪吃蛇/2048/数独等游戏的**核心状态机**（棋盘/方块/合法移动/得分/IAA 逻辑）抽成 `shared/games/<game-name>.ts` 纯函数模型，两端各自渲染。

**评估**：
- 改动量：**重大重构**（涉及 100+ 文件），需要充足的测试回归。
- 风险：**中高**（需要 e2e 测试 + 小程序云函数联调）。
- 预期收益：CRUD 模块可减少 **70-80%** 手工 view（~1500 行），AI 工具模块可减少 **60-70%**（~1200 行），游戏状态机可抽取 **40-50%**（~800 行）。
- 阶段 3 完成后，**累计可节省 55-65%** 的跨端重复代码。

### 阶段收益汇总

| 阶段 | 投入（人天） | 节省代码累计 | 风险 |
|---|---|---|---|
| 1. 直接抽取 | 2-3 天 | ~30-35% | 零 |
| 2. Adapter 模式 | 3-5 天 | ~50-55% | 低 |
| 3. 一端为主重构 | 10-15 天 | ~55-65% | 中高 |
| 长期维护节省 | — | 后续每新增功能节省 30-50% | — |

---

## 五、最佳复用架构建议

### 方案对比

| 维度 | A. 小程序优先（推荐当前） | B. Web 优先 | C. 彻底分离 |
|---|---|---|---|
| **共享范围** | shared 放置无平台依赖部分；小程序特有（tabBar/登录/支付）保留在 mini | shared 放置无平台依赖部分；Web 特有（路由守卫/组件）保留在 web | 两端各自维护，少量类型共享 |
| **契合现状** | 高：当前小程序 schema 层更成熟（crud-schema / subject-schema / quicktool-schema） | 中：Web 的组件/Tailwind 更成熟 | 低 |
| **schema 引擎迁移** | 直接把现有小程序 schema 提升为 shared，Web 新建渲染器 | 需要把 schema 从 JSDoc 改写成 TS 装饰器模式 | 各自实现 |
| **游戏引擎** | 从小程序 Canvas 游戏提炼状态机 → Web 用同一份逻辑 + DOM 渲染 | 反向迁移 | 各自实现 |
| **维护成本** | 中：小程序优先要求 shared 落地时兼顾两端能力 | 中：Web 优先，小程序部分功能不完整时难适配 | 高：三端独立，重复代码无法收敛 |
| **适合场景** | 业务以小程序为主、Web 端是补充管理工具的全场景产品 | Web 为主 + H5 小程序为辅助分发渠道 | 两套产品路线差异很大的情况 |

### 推荐架构：**小程序优先 + shared 分层**

```
work-system/
├── shared/                      # 零平台依赖的纯 TS
│   ├── constants/               # 已完成 ✓
│   ├── types/                   # 已完成 ✓
│   ├── validators/              # 已完成 ✓
│   ├── schemas/                 # ★ 下一步重点 (3 个 JS → TS 并提升)
│   │   ├── crud.ts
│   │   ├── subject-tools.ts
│   │   └── quicktool.ts
│   ├── auth/                    # ★ 阶段 2 重点
│   │   ├── machine.ts           # 状态机 + 动作签名
│   │   └── storage-adapter.ts   # 平台抽象接口
│   ├── api/
│   │   └── contracts.ts         # 端到端类型安全 API 契约
│   ├── utils/
│   │   ├── sse-parser.ts        # ★ 阶段 1 立即抽取
│   │   ├── game-helpers.ts
│   │   ├── student.ts
│   │   ├── general.ts
│   │   ├── export-data.ts       # 文档内容生成
│   │   └── image-spec.ts        # 压缩参数 + 类型
│   └── games/                   # ★ 阶段 3 游戏状态机
│       ├── snake.ts
│       ├── tetris.ts
│       ├── gobang.ts
│       └── ...
├── mini-program/                # 小程序实现层
│   ├── src/
│   │   ├── common/
│   │   │   ├── request.js       # 保留：wx.cloud transport (引用 shared contracts)
│   │   │   ├── store.js         # 改为 auth-machine 的 reactive adapter
│   │   │   ├── validators.js    # 已完成：重新导出 ✓
│   │   │   ├── feature.js       # 已完成：重新导出 ✓
│   │   │   ├── crud-schema.js   # ★ 先 → shared/schemas/crud.ts，再重新导出
│   │   │   ├── subject-schema.js
│   │   │   └── game.js          # useGame + GAME_COLORS 留小程序；rand/shuffle → shared
│   │   ├── pages/               # 页面（各游戏/工具保留平台实现）
│   │   └── components/          # uni-app 专属组件
├── web-app/
│   ├── src/
│   │   ├── api/                 # request.ts 保留 transport；teacher.ts 引用 shared contracts
│   │   ├── stores/auth.ts       # 改为 auth-machine 的 Pinia adapter
│   │   ├── views/               # 保留 Tailwind + lucide-vue-next 组件
│   │   │   ├── crud/            # ★ 可选：新建 schema-driven 通用 CRUD 渲染器（阶段 3）
│   │   │   └── games/           # 各游戏 view（DOM 渲染 + shared 状态机）
│   │   ├── components/          # Tailwind 专属组件
│   │   └── composables/
│   │       ├── useClasses.ts    # 接入 shared auth-machine
│   │       └── usePhotoUpload.ts # 引用 shared/image-spec
└── server/                      # 保持不变（后端共享层有效）
```

### 关键设计原则

1. **"shared 只放确定无平台依赖的代码"**：一旦进入 shared，不允许 import 任何 `uni.*` / `window.*` / `document.*` / `wx.*`。
2. **"adapter 薄、shared 厚"**：两端 platform adapter 代码不超过原文件 20%，核心逻辑必须在 shared。
3. **"先用 paths 别名，再考虑 npm 包"**：当前通过 tsconfig paths 直接引用 shared 源码已满足需求，不需要预构建 + 发布 @gardener/shared 包（节省 CI 复杂度）。
4. **"类型优先"**：API 契约（endpoint + 参数 + 返回）在 shared/api-contracts.ts 先落地，shared 的类型文件是三端类型同步的"第一道关卡"。

---

## 六、按 ROI 排序的前 5 个复用动作

> 排名综合考虑**节省代码量 + 实现难度 + 降低未来维护成本**。

| 排名 | 动作 | 预估节省 | 难度 | 具体步骤 |
|---|---|---|---|---|
| **1** | **抽取 `shared/utils/sse-parser.ts`** | ~100 行 | 低 | 合并 `api/teacher.ts` 的 `aiChatStream` 与 `request.js` 的 `streamChat` → 一份 `parseSSE(stream, onDelta)` 通用实现；两端各自传入自己的流 adapter。**立即可做。** |
| **2** | **把 `crud-schema.js` 提为 `shared/schemas/crud.ts`** | ~450 行 | 低 | 将 458 行纯 JS 配置转成 TypeScript，放入 shared；小程序原文件改为重新导出。Web 端可在未来直接引用这份 schema 渲染通用 CRUD 列表/表单。**阶段 1 第一天即可完成。** |
| **3** | **把 `subject-schema.js` 提为 `shared/schemas/subject-tools.ts`** | ~250 行 | 低 | 同上。14+ 学科工具 + prompt 模板全部共享。Web 端可引用生成跨端一致的 AI prompt。 |
| **4** | **抽取 `shared/auth-machine.ts`（鉴权状态机接口）** | ~200-300 行 | 中 | 定义 `AuthState`/`AuthActions`/`FeatureProfile` 类型 + 纯函数 reducer；`stores/auth.ts` 用 Pinia 实现、`store.js` 用 reactive 实现，共享逻辑零漂移。需要约 2 天。 |
| **5** | **抽取 `shared/contracts/api.ts`（API 端到端类型安全契约）** | ~150-200 行 + 类型保护 | 中 | 结合后端 swagger（`.fetch/swagger.json`）+ 现有各 `api/*.ts` 中的函数签名，生成一个 `shared/api/endpoints.ts` 列出所有 endpoint 的 method/path/params/response 类型。两端请求层从同一份契约推导类型，**后端增删接口时三端同时报错**，消除联调漂移。 |

### 首推执行顺序

```
Day 1 : 动作 #1（SSE parser）+ 动作 #2（crud-schema 提升）= ~550 行节省
Day 2 : 动作 #3（subject-schema 提升）+ 动作 #5 启动（API 契约骨架）
Day 3-4 : 动作 #4（auth-machine）+ 动作 #5 完善
```

完成以上 5 个动作后，预计 **4-5 个工作日内可节省 1000-1200 行跨端重复代码，并消除 SSE 流式/鉴权漂移/schema 不一致三类最常见的跨端 bug 来源**。

---

*报告基于截至 2026-03-17 的代码快照。随着功能迭代，建议每季度重新执行一次此分析的"现状"章节。*
