# Design System — 家长端看板

> K12 校园管理工作台 · 家长端看板设计规范（唯一视觉源文件）
> 依据：2026-07-29《家长端看板设计与缺陷盘点报告》（产品评审 + 设计审查）已评审结论。
> 贯穿约束（用户硬性规则）：**所有前端改动，Web 端（web-app，Vue3 + Tailwind）与小程序端（mini-program，uni-app）必须在「功能与表现」上保持一致。**
> 关联产物：`deliverables/gstack/parent-dashboard-design-review-2026-07-29.md`、`deliverables/gstack/parent-dashboard-prototype.html`（Web 端基准示例）。

---

## 1. 图标系统（lucide ↔ emoji 1:1 映射）

### 1.1 原则：语义唯一（One Semantic = One Icon Pair）

- **emoji 负责「模块识别」**：同一语义在两端永远对应同一个 emoji（小程序直接渲染 emoji；Web 端用同语义的 lucide 图标，肉眼等价）。
- **颜色负责「语义状态」**，不靠图标区分好坏：
  - 🟢 **绿** = 正向 / 完成 / 高亮 / 进步
  - 🟡 **琥珀（黄）** = 待办 / 警示 / 作业
  - 🌸 **粉** = 通知 / 爱心 / 关注
  - 🔵 **蓝** = 信息 / 考勤 / 相册
  - 🔴 **红** = 风险 / 薄弱 / 逾期
- **成对出现，不可漏配**：一端用 lucide，另一端必须用对应 emoji；禁止一端有图标而另一端缺失或语义漂移。
- 同一个看板里，emoji 只在「模块识别」层出现一次（如卡片左上角），不把 emoji 当状态色用——状态一律交给 §3 的语义色 token。

### 1.2 映射表（lucide → emoji）

| 语义 | Web（lucide） | 小程序（emoji） | 默认语义色 |
|------|---------------|----------------|-----------|
| 成绩趋势 | `TrendingUp` | 📈 | 绿 |
| 排名 | `Trophy` | 🏆 | 绿 |
| 雷达（能力维度） | `Radar` | 🕸️ | 绿（描边）/ 灰（对照） |
| 考勤 | `CalendarCheck` | 📅 | 蓝 |
| 出勤 | `CircleCheck` | ✅ | 绿 |
| 作业 | `BookOpen` | 📝 | 琥珀 |
| 奖惩 | `Medal` | 🏅 | 绿 / 红（按性质） |
| 成长 | `Sprout` | 🌱 | 绿 |
| 相册 | `Camera` | 📷 | 蓝 |
| 课表 | `CalendarDays` | 🗓️ | 蓝 |
| 值日 | `Broom` | 🧹 | 琥珀 |
| 优势（进退步） | ⬆️（绿箭头） | ⬆️ | 绿 |
| 薄弱（进退步） | ⬇️（红箭头） | ⬇️ | 红 |
| 公告 | `Bell` | 📢 | 粉 |
| 提醒 | `BellRing` | 🔔 | 粉 |
| 家长中心 | `Home` | 🏡 | 绿（品牌） |
| 欢迎 | `Heart` | 💗 | 粉 |

> 进退步箭头（⬆️/⬇️）属于「数据状态指示」，颜色固定（绿=优势、红=薄弱），不进入模块识别层；它们通常出现在成绩趋势卡、雷达卡、优弱势看板中，与 §2 的图表高亮逻辑共用同一套语义色。

---

## 2. 五类卡片规范（双端同构）

所有卡片统一基底：白底（或 cream-100 `#FAF6EB`）、圆角 **16px / 32rpx**、柔和阴影 `0 4px 16px rgba(157,116,80,0.08)`（cocoa 暖调投影）、内边距 16–20px。同一看板两端**组件类型、出现顺序、文案标签、语义色**逐一对齐。

### ① 概览数字卡（Overview Number Card）
- **结构**：图标（emoji / lucide）+ 标签 + 大数字（30px / 44–56rpx，字重 800）+ 趋势小标。
- **四卡基准配色**（以「成绩趋势看板」为例）：
  | 卡 | 标签 | 语义色 | 说明 |
  |----|------|--------|------|
  | 1 | 待读通知 | 粉（`sakura-500` / `--c-pink`） | 通知类 |
  | 2 | 待完成作业 | 琥珀（`--p-amber` / `--c-accent`） | 待办类 |
  | 3 | 考试次数 | 绿（`--p-green` / `--c-primary`） | 正向类 |
  | 4 | 最新排名 | 绿（`--p-green` / `--c-primary`） | 正向类（进步时显示 ⬆️） |
- 图标底色用对应语义色的柔底（如粉=`#FCE9EE`、琥珀=`rgba(230,162,60,.12)`、绿=`#E8F5E9`）。
- 趋势小标用同色文字 + ⬆️/⬇️ 箭头，红仅用于「退步/逾期」。

### ② 走势图卡（Trend Chart Card）
- **标题行**：左侧 📈 + 「成绩趋势」；右侧环比小标（如 `环比 ▲ +6.4%`，绿）。
- **渲染技术**：Web 用 **SVG 折线**；小程序用 **canvas**（或 CSS）。两端技术可不同，但下列参数**必须同构**：
  - **学生线**：`#07c160`，**实线**，**线宽 2.5px**，圆形实心数据点（r≈4，填充 `#07c160` + 白色描边）。
  - **班级均分线**：**灰色虚线**（`stroke-dasharray: 6 6`），线宽 1.5px，方形/菱形灰点。
  - **图例**：学生（绿实线）/ 班级均分（灰虚线），两端文案与顺序一致。
  - Y 轴刻度、网格线、X 轴考试名标签两端对齐。
- **交互（建议）**：数据点 hover 显示tooltip（考试名 / 总分 / 班级均分 / 差值）；图例可点击切换班级线显隐。

### ③ 进度环卡（Progress Ring Card）
- **渲染技术**：Web 用 **SVG `circle` + `stroke-dasharray`**；小程序用 **`conic-gradient`**。
- **阈值（语义色硬规则）**：
  - 🟢 **绿** ≥ 80%
  - 🟡 **琥珀** 60%–80%
  - 🔴 **红** < 60%
- 环宽统一（建议 10–12px / 20–24rpx），环心显示百分比大数字 + 标签。两端环底色（track）统一用浅灰/浅绿。

### ④ 雷达卡（Radar Card）
- **渲染技术**：Web 用 **SVG 多边形**；小程序用 **canvas**。
- **学生面**：绿色**半透明填充**（`rgba(7,193,96,.18)`）+ **绿色描边**（`#07c160`，线宽 2px）。
- **班级均分面**：**灰色描边**（无填充或极淡灰），线宽 1.5px。
- 维度轴标签（如 语文/数学/英语/…）两端一致；同看板两端轴数、顺序相同。

### ⑤ 分布对比卡（Distribution Compare Card）
- **渲染技术**：两端统一用 **CSS 柱状**（同构，不混用图表类型）。
- **高亮逻辑**：**学生所在分数段高亮 `#07c160`**；其余段用**浅绿 `#E8F5E9`**。
- 柱下方标注分段区间；图例说明「● 孩子所在段」。两端柱子数量、区间、高亮段必须一致。

---

## 3. 双端 Token 映射表

> 换算基准：小程序设计稿以 **750rpx** 宽，对应 Web 375px 视口 → **1rpx = 0.5px**（即 2rpx = 1px）。

### 3.1 颜色 Token

| 语义 | Web token | 小程序 token | 统一取值 | 备注 |
|------|-----------|-------------|---------|------|
| 品牌绿（高亮/进度/正向） | 新增 `--p-green:#07c160`<br>新增 `--p-green-soft:#E8F5E9` | `var(--c-primary)`（已为 #07c160） | **#07c160** | Web 原 `mint`（≈#2E8B57）降为次要绿，品牌绿统一为 #07c160 |
| 琥珀（待办/警示/作业） | 新增 `--p-amber:#E6A23C` | `var(--c-accent)`（#e6a23c） | **#E6A23C** | — |
| 粉（通知/爱心） | `sakura-500` / `sakura-100` | 新增 `--c-pink`（字 #C9436D / 底 #FCE9EE） | 字 #C9436D / 底 #FCE9EE | Web 柔底用 `sakura-100`，字用 `sakura-500` |
| 蓝（信息/考勤/相册） | `sky2-500` / `sky2-100` | 新增 `--c-blue`（字 #1C6FB3 / 底 #E0F0FC） | 字 #1C6FB3 / 底 #E0F0FC | Web 柔底用 `sky2-100`，字用 `sky2-500` |
| 红（风险/薄弱/逾期） | 建议新增 `--p-danger:#f56c6c` | `var(--c-danger)`（#f56c6c） | **#f56c6c** | 小程序已存在；Web 建议补 `--p-danger` 以对称 |
| 暖底 | `cream-50 #FDFBF4` / `cream-100 #FAF6EB` | `--c-card2 #f8f4ec` | 米黄奶油底 | 页面/卡片底色 |
| 文字 | `cocoa-700 #6B4C34` / `cocoa-800 #543C28` | 对应深棕 | 可可棕文字 | — |

### 3.2 圆角 / 间距 / 字号 Token

| 维度 | Web | 小程序 | 统一 |
|------|-----|--------|------|
| 卡片圆角 | 16px（`rounded-2xl`） | 32rpx | 16px ↔ 32rpx |
| 横幅圆角 | 24px（`rounded-3xl`） | 48rpx | 24px ↔ 48rpx |
| 间距阶梯 | 4 / 8 / 12 / 16 / 20 / 24 px | 8 / 16 / 24 / 32 / 40 / 48 rpx | 逐档 1:2 |
| 标题 | 18 / 20 px | 36 / 40 rpx | 18/20px ↔ 36/40rpx |
| 区块标题 | 16 px | 32 rpx | 16px ↔ 32rpx |
| 正文 | 14 px | 28 rpx | 14px ↔ 28rpx |
| 大数字 | 30 px | 44–56 rpx | 30px ↔ 44–56rpx |

### 3.3 落地动作（前端执行，非本文件范围）
- Web `web-app/index.html`：在 CSS 变量中新增 `--p-green`、`--p-green-soft`、`--p-amber`、`--p-danger`，并保留 `mint` 为次要绿。
- 小程序 `mini-program/src/App.vue`：在 `:root`/页面变量中新增 `--c-pink`、`--c-blue`（按 §3.1 取值）。
- 两端均在 `.dark` 下补上述新增 token 的暗底变体（评审发现 #16）。

---

## 4. 一致性铁律（Hard Rules）

1. **组件同构**：同一看板，两端**组件类型、出现顺序、文案标签、语义色**必须逐一对齐；禁止一端有某卡片而另一端缺失或改名。
2. **图表同构**：图表渲染技术可不同（Web SVG / 小程序 canvas·CSS），但 **颜色、线宽、圆角、标签、高亮逻辑必须同构**；同一看板两端**必须用同一种图**（如一端折线，另一端也必须是折线，不得一端折线一端柱状）。
3. **图标成对**：一端 lucide 则另一端 emoji 同一语义（§1.2），不得漏配或语义漂移。
4. **语义色不靠图标**：状态（正向/待办/通知/信息/风险）一律由 §3 的语义色 token 表达，emoji 只做模块识别。
5. **数据隔离红线**：所有家长数据面须按 `studentId` / `classId` 隔离，禁止越权查看他人孩子数据（工程红线 D13）。
6. **多端同步提交**：任何家长端改动，Web 与小程序须在同一次需求/PR 中同步完成，并互相验证表现一致。
7. **暗色一致**：新增 token 在两端 `.dark` 均需提供暗底变体，避免暗色下语义色失效。

---

*本文件为家长端看板视觉唯一源；任何 UI 实现（含 `deliverables/gstack/parent-dashboard-prototype.html` 基准示例）均以本文件为准。*
