# 依赖（体积大，可重装，不入库）
node_modules/
dist/
# 以下已随遗留产物清理（2026-08-02），目录不再存在，条目保留无影响：
# app/node_modules/
# app/dist/
# dist-electron/
# dist-electron-new/
.seed-dist/
server/.seed-dist/
seed-manifest.json

# 日志 / 运行时文件
*.log
*.pid
app.log
app.pid
.env
# 环境变量（可能含密钥，勿入库；保留示例文件供参考）
.env.*
.env.local
!.env.example
seed-runner.js
tester.js
test-results.json
test-report.json

# 测试覆盖率报告（本地生成，勿入库）
coverage/
.nyc_output/

# WorkBuddy 项目数据（非源码）
.workbuddy/

# 操作系统 / 编辑器
.DS_Store
Thumbs.db
.idea/
.vscode/

# app/ 内部已自带 .gitignore

# QA 测试运行时产物（含 JWT token，不入库）
e2e/qa/qa-env.json

# 测试临时目录与含凭证文件（勿入库）
.repair-tmp/
.repair-tmp2/
scripts/mini-sa.env
scripts/mini-test-tokens.json
