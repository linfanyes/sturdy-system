# 依赖（体积大，可重装，不入库）
node_modules/
dist/
# 以下已随遗留产物清理（2026-08-02），目录不再存在，条目保留无影响：
# app/node_modules/
# app/dist/
# dist-electron/
# dist-electron-new/
.seed-dist/
server/.seed-dist/
seed-manifest.json

# 日志 / 运行时文件
*.log
*.pid
app.log
app.pid
.env
# 环境变量（可能含密钥，勿入库；保留示例文件供参考）
.env.*
.env.local
!.env.example
seed-runner.js
tester.js
test-results.json
test-report.json

# 测试覆盖率报告（本地生成，勿入库）
coverage/
.nyc_output/

# WorkBuddy 项目数据（非源码）
.workbuddy/

# 操作系统 / 编辑器
.DS_Store
Thumbs.db
.idea/
.vscode/

# app/ 内部已自带 .gitignore

# QA 测试运行时产物（含 JWT token，不入库）
e2e/qa/qa-env.json

# 测试临时目录与含凭证文件（勿入库）
.repair-tmp/
.repair-tmp2/
scripts/mini-sa.env
scripts/mini-test-tokens.json
# QA 测试脚本与结果产物（含生产地址/账号口令/令牌，勿入库）
scripts/mini-api-test-results.json
scripts/mini-api-test.mjs
scripts/mini-healthcheck.mjs
scripts/mini-login.mjs
scripts/mini-page-extract.mjs
scripts/mini-pages.json
scripts/mini-provision.mjs
scripts/gen-mini-report.mjs
scripts/gen-test-case-doc.mjs
scripts/gen-test-data.mjs
scripts/repair-e2e.mjs
# 本机 H5 重建时的旧产物备份目录（rename 大法绕过 safe-delete 用）
.h5-old-*/

# 构建残留（IDE 锁定时 mv 出的旧 dist，勿入库）
dist-*/

# 已废弃的小程序旧页面（源码树保留但不跟踪，减小仓库体积）
mini-program/src/_deprecated_orphan_pages/

# TypeORM 无需提交编译产物
server/dist/
