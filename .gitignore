# 依赖（体积大，可重装，不入库）
node_modules/
dist/
# 以下已随遗留产物清理（2026-08-02），目录不再存在，条目保留无影响：
# app/node_modules/
# app/dist/
# dist-electron/
# dist-electron-new/
.seed-dist/
server/.seed-dist/
seed-manifest.json

# 日志 / 运行时文件
*.log
*.pid
app.log
app.pid
.env
# 环境变量（可能含密钥，勿入库；保留示例文件供参考）
.env.*
.env.local
!.env.example
seed-runner.js

# WorkBuddy 项目数据（非源码）
.workbuddy/

# 操作系统 / 编辑器
.DS_Store
Thumbs.db
.idea/
.vscode/

# app/ 内部已自带 .gitignore

# QA 测试运行时产物（含 JWT token，不入库）
e2e/qa/qa-env.json

# 测试临时目录与含凭证文件（勿入库）
.repair-tmp/
.repair-tmp2/
scripts/mini-sa.env
scripts/mini-test-tokens.json
# Playwright CLI 临时截图快照（本地调试产物，勿入库）
.playwright-cli/

# Swagger 临时拉取文件
.fetch/

# Lighthouse / 诊断产物（本地生成，勿入库）
web-app/public/diag.html

# 一次性补丁脚本（已执行完毕，勿入库）
e2e/qa/patch-tests.cjs
e2e/qa/patch-tests2.cjs

# 一次性页面/脚本生成器（已执行完毕，勿入库）
scripts/all_phases.py
scripts/create_missing_pages.py
scripts/fix_api_paths.py
scripts/phase1.py

# 全量测试用例文档（随测试代码维护，不再单独存档）
web-app/test/Test-Cases-Full.md
server/test/Test-Cases-Full.md
mini-program/test/Test-Cases-Full.md

# 历史审计/报告文档（已清理，不再入库）
web-app/PRELAUNCH_QA_REPORT.md
web-app/TEST_CASES.md
web-app/TEST_REPORT.md
web-app/CONSISTENCY_AUDIT.md
web-app/OPTIMIZE_CHECKLIST.md
web-app/CI.md
server/test/perf-benchmark.ts
server/test/frontend-static-analysis.js
# 本机 H5 重建时的旧产物备份目录（rename 大法绕过 safe-delete 用）
.h5-old-*/

# 构建残留（IDE 锁定时 mv 出的旧 dist，勿入库）
dist-*/

# 已废弃的小程序旧页面（源码树保留但不跟踪，减小仓库体积）
mini-program/src/_deprecated_orphan_pages/

# TypeORM 无需提交编译产物
server/dist/

# QA 构建产物
server/.qa-dist/

# Docker 构建依赖：放行 server/vendor/gardener-shared/dist（@gardener/shared 编译产物）。
# 该目录须随仓库提交，否则云端构建上下文取不到 dist，TS 会报 Cannot find module '@gardener/shared/...'。
# 注意：否定规则必须放在 dist/ 忽略规则之后才生效。
!server/vendor/gardener-shared/dist/
!server/vendor/gardener-shared/dist/**

# E2E 运行产物与依赖（脚本本体如 e2e/*.mjs 仍可正常入库）
e2e/node_modules/
e2e/reports/

# 小程序接口/页面巡检的本机输出（每次运行都会变，勿入库）
scripts/mini-api-test-results.json
scripts/mini-pages.json
*.tsbuildinfo

# 云端测试运行态/缓存产物（含 JWT token，勿入库）
test/cloud/.gen-state.json
test/cloud/.cloud-sweep-result.json

# P0-10修复：补充遗漏的构建产物与缓存目录
web-app/dist/
mini-program/dist/
web-app/.vite/
mini-program/.vite/
*.local
.catpaw/
# ESLint 缓存
.eslintcache
# Prettier 缓存
.prettiercache
