# 小程序改造差异清单 v2（基于全量重新扫描）

> 本次扫描不依赖旧改造文档，直接对比 `app/`（web）与 `mini-program/`（小程序）源码。
> 扫描范围：web 28 业务视图 + 45 工具 + 19 游戏 + 22 store + 21 后端模块；小程序 73 页面 + 9 common 模块 + 1 公共组件。

## 一、覆盖率总览

| 维度 | web 数量 | 小程序对应 | 覆盖率 |
|---|---|---|---|
| 业务视图 | 28 | 26（无 NotFound/DevSeed） | 100%（其余均迁移） |
| 工具页 | 45 | 43（缺 stroke-order/color-pick） | 95.6% |
| 游戏页 | 19 | 19 | 100% |
| 后端模块 | 21 | 21 | 100% |
| Store 状态 | 22 | 等价 reactive + 本地存储 | 100% |

## 二、缺失/不完善功能清单

### P0（重要功能缺失，必须补）

#### P0-1 计分板（ScorePanel）专用页
- **web**: `views/tools/ScorePanel.vue` 多组计分（最多 6 组）+ 自定义名称/颜色 + 加减分按钮（+1/+5/+10/-1/-5/-10）+ 历史记录 + 撤销 + 全屏模式
- **小程序现状**: 通过 `crud:group-scores` 渲染，只能看列表+增删改，无加减分按钮、无历史、无撤销
- **改造**: 新建 `pages/tools/score-panel/score-panel.vue`，多组计分 + 加减分 + 历史 + 撤销；后端复用 `class-score` 模块

#### P0-2 奖励兑换（Reward）专用页
- **web**: `views/tools/Reward.vue` 学生选择 + 奖励项目 + 兑换记录表格 + 库存管理 + 兑换统计图 + 批量发放
- **小程序现状**: 通过 `crud:reward-records` 渲染，仅基础 CRUD
- **改造**: 新建 `pages/tools/reward/reward.vue`，兑换记录 + 库存 + 统计 + 批量发放

### P1（细节不完善，应补）

#### P1-1 计算器（calc）历史记录
- **web**: `views/tools/Calc.vue` 有历史记录列表 + 科学计算 Tab
- **小程序现状**: `pages/tools/calc.vue` 仅标准计算器，无历史
- **改造**: 增加历史记录列表（本地存储，最近 20 条），点击可重用

#### P1-2 公告「使用模板」按钮
- **web**: `views/Notice.vue` 「使用模板」按钮 → 模板库选择 → 一键填充
- **小程序现状**: `pages/notice/notice.vue` 无模板入口
- **改造**: 公告表单顶部增加「📋 使用模板」按钮，弹模板列表（拉 `/notice-templates`），选中后填充标题+内容

#### P1-3 成绩综合分析「各科雷达图」
- **web**: `views/GradeManage.vue` 分析 Tab 有各科雷达图（5 维）
- **小程序现状**: `pages/grades/grades.vue` 综合分析弹层只有 10 段柱状图，无雷达
- **改造**: 综合分析弹层增加「各科对比雷达图」（canvas 渲染，维度：均分/及格率/优秀率/最高/最低）

#### P1-4 工作台「学生生日提醒」卡片
- **web**: `views/Dashboard.vue` 有「学生生日提醒」列表
- **小程序现状**: `pages/dashboard/dashboard.vue` 无生日提醒
- **改造**: 增加「🎂 本周生日」卡片，列出 7 天内过生日的学生（按 `birthDate` 计算）

#### P1-5 作业「分析视图」
- **web**: `views/Homework.vue` 有 Tab「作业分析」（完成率柱状图 + 未完成学生 + 提醒）
- **小程序现状**: `pages/homework/homework.vue` 仅列表，无分析
- **改造**: 增加 seg「📊 完成分析」，按班级统计完成率柱状图 + 未完成学生名单

#### P1-6 笔顺演示（stroke-order）工具
- **web**: `views/tools/StrokeOrder.vue` SVG 笔顺动画 + 逐笔/连续模式 + 朗读拼音
- **小程序现状**: 缺失
- **改造**: 新建 `pages/tools/stroke/stroke.vue`，简化为「汉字 → AI 生成笔顺描述 + 朗读拼音」（小程序 SVG 笔顺动画复杂，用 AI 生成笔顺说明 + 拼音朗读替代）

### P2（已有可加强，时间允许再做）

#### P2-1 设置页「使用帮助」入口
- **web**: `components/SystemManual.vue` 全局说明书 Modal
- **小程序现状**: 无
- **改造**: config 页增加「📖 使用帮助」入口，弹 sheet 展示功能索引 + 快捷操作说明

#### P2-2 学生列表搜索防抖
- **web**: `useDebouncedSearch` 200ms 防抖
- **小程序现状**: `pages/students/students.vue` 搜索直接 watch，无防抖
- **改造**: 增加 200ms 防抖

#### P2-3 颜色选择器（color-pick）
- **web**: `views/tools/ColorPick.vue` HEX/RGB/CMYK + 17 色调色板
- **小程序现状**: 缺失
- **改造**: 暂不补，使用场景极少（小程序无前端取色需求）。可在班级颜色 picker 里增加预设色板（已有 8 色）。

#### P2-4 虚拟滚动
- **web**: `VirtualList.vue`
- **小程序现状**: students 用分页（PAGE_SIZE=20）
- **结论**: 小程序分页足够，不需要虚拟滚动

## 三、本次改造范围

按优先级执行：P0-1 → P0-2 → P1-1 → P1-2 → P1-3 → P1-4 → P1-5 → P1-6 → P2-1 → P2-2 → build → 提交
