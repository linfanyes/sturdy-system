# 园丁工作台 · 启动 / 迁移工具集

> Vue 3 + TypeScript + Vite + Pinia + Tailwind CSS
> 教师专用一站式教学管理 Web 应用（PWA 离线可用）

**v3 优化版** — 本次更新修复了 5 项 P0 级 Bug、接入备份历史界面、修复内存泄漏、添加 Brotli 压缩与 vendor 拆分、优化 PWA 缓存策略、样式去重等共 11 项优化（详见文末更新日志）。

本目录是项目的**可移植启动包**。所有项目文件均统一存放在 `app/` 子目录中，并使用相对路径，**整个文件夹可整体迁移到任意位置运行**，无需重新配置。

---

## 📁 目录结构

```
workSystem/
├── app/                        # 项目源代码（Vue 3 应用）
│   ├── src/                    # 源代码
│   ├── public/                 # 静态资源
│   ├── docs/                   # 项目文档（PRD / 技术架构）
│   ├── index.html              # 入口 HTML
│   ├── package.json            # 依赖与脚本
│   ├── package-lock.json       # 依赖锁定文件
│   ├── tsconfig.json           # TypeScript 配置
│   ├── vite.config.ts          # Vite 配置（已使用相对路径）
│   ├── tailwind.config.js      # Tailwind 主题
│   ├── postcss.config.js       # PostCSS 配置
│   ├── .gitignore              # Git 忽略
│   └── .vscode/                # VSCode 推荐扩展
├── start.bat                  # 一键启动（Windows，双击即可）
├── stop.bat                   # 一键停止（Windows）
├── setup-hosts.bat            # 可选：配置 teacherWorkStation 本地域名
├── app.log                     # 启动日志（运行后自动生成）
├── app.pid                     # 进程 PID（运行后自动生成）
└── README.md                   # 本文件
```

> 💡 **相对路径说明**：`app/vite.config.ts` 已配置 `base: './'`、`outDir: 'dist'`，可在任意根目录下运行。

---

## 🚀 快速开始（Windows）

1. 安装 **Node.js 18+**（https://nodejs.org）
2. 双击 `start.bat`
3. 等待几秒，浏览器自动打开 **http://localhost:5201**
4. 输入称呼 + 登录码 `1314520` 登录

> 首次启动会自动 `npm install` 安装依赖（1-3 分钟），后续启动秒开。
> 若想使用本地域名 `http://teacherWorkStation:5201`，可右键以管理员身份运行 `setup-hosts.bat`。

### 启动模式

`start.bat` 支持以下参数：

| 参数       | 说明                                    |
| ---------- | --------------------------------------- |
| `dev`      | 开发模式（默认），含热更新，端口 5201    |
| `preview`  | 先 `npm run build` 再启动 preview       |
| `build`    | 仅构建生产版本，输出到 `app/dist/`      |
| `install`  | 仅安装依赖，不启动服务                  |

示例：
```bat
start.bat preview
```

### 自定义端口

通过环境变量 `PORT` 可指定端口：
```bat
:: CMD
set PORT=8080 && start.bat

:: PowerShell
$env:PORT=8080; .\start.bat
```

### 停止服务

双击 `stop.bat`，脚本会按 `app.pid` 优雅关闭服务。

---

## 🔑 登录与初始化

- **登录码**：`1314520`（请勿修改）
- **首次登录**：会弹出"首次设置"窗口，要求选择**任教学期**（如 2026 春季学期）和**任教学科**（多选，可在个人中心修改）
- **任教学期**：默认根据当前日期推断 2026 春/秋，可在工作台右上角切换
- **任教学科**：决定课表/作业/成绩等模块默认展示的科目范围

---

## 🛡️ 按登录人数据隔离

每位老师登录后只会看到自己维护的班级 / 学生 / 成绩 / 作业 / 课表 / AI 配置 等所有信息。**不同老师在同一台电脑上轮换使用，系统数据互不共享、互不可见。**

### 工作机制

- 系统为每位老师生成独立的 `userId`（保存在 `trace-user` 中）
- 所有业务数据使用 `trace.{userId}.{base}` 形式命名（`base` 为模块名）
- 登出后该老师数据仍保留；下次再登录自动恢复
- 支持多账号轮换：例如李老师登录 → 登出 → 王老师登录，**两人数据完全独立**

### 已隔离的模块

| 模块         | localStorage key                       |
| ------------ | -------------------------------------- |
| 班级 / 学生  | `trace.{userId}.classes`               |
| 课表 / 作业 / 公告 / 资源 / 考勤 | `trace.{userId}.school`  |
| 考试计划     | `trace.{userId}.exams`                 |
| 成绩         | `trace.{userId}.grades`                |
| 笔记         | `trace.{userId}.notes`                 |
| 待办         | `trace.{userId}.todos`                 |
| 奖惩         | `trace.{userId}.rewards`               |
| 同事 / 通讯录 | `trace.{userId}.teachers`             |
| AI 配置      | `trace.{userId}.ai-settings`           |
| AI 对话      | `trace.{userId}.ai-chats`              |

> 通用身份信息（当前登录人、登录历史）使用全局 key `trace-user` / `trace-user-history`。

---

## 📚 主要功能

### 🏠 工作台
今日课表、今日待办、学期切换、近期动态

### 🏫 班级与学生
- 班级管理：增删班级、设置班主任、学期设置（选择本学期涉及学科）
- 学生管理：增删改学生、批量导入（**TXT/Excel 模板 + AI 智能识别**）
- 班级整体迁移：把某班级及学生整体迁移到另一个班级

### 📅 我的课表
- 课表展示：1 张表，每日每课显示班级+任课科目，同一时间多个班级堆叠
- 课表排版：可设置任课、午自习、课后服务；编辑以草稿形式暂存，点击"保存到课表"才生效
- 班级课表：每个班级独立课表，支持批量导入（**TXT/Excel 模板 + AI 智能识别**）
- 默认 1-7 天都有课，午自习/课后服务默认显示班主任

### 📊 成绩管理
- 新增考试：语数英默认满分 100，其他默认 50，支持自定义
- 录入成绩：单科录入 / 多科批量导入（**TXT/Excel 模板 + AI 智能识别**）
- 综合分析：班级总分排名、分数段分布柱状图、各科平均分对比、及格率/优秀率
- 班级分数段分布：10 段柱状图，含及格线 / 优秀线

### 📋 考试计划
新增考试时间、关联科目满分

### ✅ 考勤管理
每日打卡，迟到/旷课/请假

### 📝 作业登记
布置/登记/统计作业

### 📣 班级公告
- 支持置顶、一键结束、结束时间展示
- 折叠已结束公告

### 📒 我的笔记 / 待办 / 奖惩
笔记、待办、奖惩记录

### 📇 教师通讯录
同事信息与任教分配

### 📚 教学资源
教案、课件等资源管理

---

## 🤖 AI 对话（工具箱 → AI 对话）

> **核心特性**：AI 已自动读取本系统全部数据（班级 / 学生 / 成绩 / 课表 / 笔记等），可基于真实数据回答老师提问。

- **附件上传**：支持文本/代码（.txt .md .csv .json ...）与图片（.png .jpg）；图片走多模态识别
- **多模型兼容**：支持 OpenAI 兼容协议（默认 deepseek，可改为其他）
- **多会话**：左侧可建/删多个对话，支持搜索历史对话，支持"复制 / 添加到笔记 / 重新生成"
- **系统说明上下文**：AI 已被注入本系统使用说明书，回答时更精准
- **快捷指令**：点击输入框左侧灯泡图标，可使用预设的教学场景快捷指令（分析班级成绩、生成教案、写期末评语、出练习题、家长沟通、课堂活动、知识点梳理、作业批改）

### 快速提问示例
- "帮我总结一下三年一班期中考试情况"
- "李明的数学最近一次考试多少分？"
- "把今天的课表写一段话发给我"
- "这次考试及格率最高的科目是哪个？"

---

## ✨ AI 智能识别导入

所有批量导入（课表 / 学生 / 成绩）都支持 **AI 智能识别模式**：

- **不按模板也能导入**：把任意格式的文本粘进去，AI 自动解析为标准结构
- **流式进度**：实时显示 AI 解析状态
- **解析后预览**：自动切换到手动模式让你确认无误再保存

---

## 🧰 工具箱

工具箱按学科/用途分类，每个分类统一颜色背景，便于快速定位。

### 💬 对话工具
| 工具         | 说明                                  |
| ------------ | ------------------------------------- |
| 🤖 AI 对话   | 多模态对话，自动注入本地教学数据，支持 Markdown 渲染 |

### 📝 文字工具
| 工具         | 说明                                  |
| ------------ | ------------------------------------- |
| ✍️ 评语生成  | 按学生表现生成个性化评语              |
| 📋 通知模板  | 常用通知模板快速生成                  |
| 📑 文案模板库 | 教师常用文案模板库                   |
| 📝 优选试卷  | AI 按年级学科题型生成完整试卷          |
| 📚 优质教案  | AI 多篇候选教案，勾选整合              |
| 💡 知识点   | 知识点整理与生成                      |

### 📚 语文工具
| 工具         | 说明                                  |
| ------------ | ------------------------------------- |
| 📜 古诗词助手 | 内置 22 首古诗，按年级/主题检索，含译文赏析朗读 |
| 🎧 汉字听写  | 自定义词库，浏览器语音播报，学生默写自检 |
| 📚 阅读理解生成 | 按年级/文体 AI 生成阅读理解文章与试题 |
| ✍️ 小作文助手 | AI 生成范文、智能批改学生作文          |
| 🔤 成语词典  | 35 个成语查询、出处造句、AI 成语接龙   |
| 🎵 拼音标注  | 汉字文本自动标注拼音，支持多种模式     |
| ✏️ 汉字笔顺  | 汉字笔顺动画演示和分步练习            |
| 📖 作文素材  | 写人/记事/写景/状物四大类作文素材库   |

### 🔢 数学工具
| 工具         | 说明                                  |
| ------------ | ------------------------------------- |
| ➕ 口算生成  | 批量生成口算题，支持打印              |
| 📐 竖式计算  | 生成加减法进退位、乘法竖式、除法竖式练习题 |
| 📋 口算答题卡 | 口算答题卡生成器                      |
| 🔢 乘法口诀  | 九九乘法口诀表练习                    |
| ⚖️ 单位换算  | 长度、重量、时间等单位换算            |
| 📝 错题本    | 记录和管理学生错题                    |

### 🔤 英语工具
| 工具         | 说明                                  |
| ------------ | ------------------------------------- |
| 📚 英语单词卡片 | 小学各年级英语单词学习，支持闪卡和测验模式 |
| ✨ 英语句型练习 | 一般现在时/过去时/将来时/进行时练习    |
| 📻 英语听力  | AI 生成听力材料与题目，浏览器 TTS 朗读 |
| 🔤 英语语法练习 | 时态、句型转换、单选填空等语法专项    |
| 💬 英语情景对话 | 购物/问路/就餐等情景对话生成与角色扮演 |
| 🔡 单词拼写  | 听音拼写、看中拼英、看英写中三种模式  |
| 🎙️ 口语练习  | AI 生成朗读文本，范读跟读录音对比      |
| 📖 英语爽文  | AI 生成趣味英语故事，含生词表和翻译    |

### 🎮 游戏工具
| 工具         | 说明                                  |
| ------------ | ------------------------------------- |
| 🎮 小游戏合集 | 18 合 1：2048/扫雷/贪吃蛇/井字棋/五子棋/消消乐/打地鼠/数字华容道/俄罗斯方块/飞机大战/极速摩托/汽车躲避/24点/数独/数字排序/记忆翻牌/图片拼图/颜色反应 |
| 🌸 笑口常开  | 点击花朵绽放，放松心情                |

### 📦 其他工具
| 工具         | 说明                                  |
| ------------ | ------------------------------------- |
| 🌐 翻译      | 12 种语言互译，流式响应                |
| 🟢 黑板报生成 | 按主题 AI 生成 3 套黑板报方案，模拟黑板展示 |
| 🎯 随机点名  | 公平随机 / 权重模式                   |
| ⏱️ 倒计时   | 课堂倒计时                            |
| 🧮 课堂计算器 | 标准 + 分数运算                      |
| 🗓️ 课表排版 | 课表网格编辑器，支持任课分配          |
| 🎨 取色器   | 屏幕取色 + HEX/RGB/HSL 转换          |
| 👥 随机分组  | 学生随机分组，支持组数/每组人数       |
| 🏅 奖惩记录  | 快速记录学生奖惩                      |
| 🏆 获奖记录  | 记录荣誉，AI 识别奖状自动填写          |

---

## 📸 班级风采

> 路径：班级菜单 → 班级风采

- 按班级展示照片墙，支持导入多张照片（自动压缩）
- 点击照片可放大查看，支持左右切换、ESC 关闭
- 照片可添加描述、删除
- 按登录人 + 班级隔离存储

---

## 🤖 AI 模型透明度提示

所有调用 AI 大模型的功能页面，顶部均显示 **AI 模型信息提示条**：
- 🤖 当前文本模型（如 `qwen3.7-plus`）
- 👁️ 当前多模态模型（如 `qwen3-vl-plus`）
- ✅ 是否已注入本地数据

让教师清楚知道 AI 用的是什么模型、数据来源，增强透明度与信任感。

---

## 🌐 本地域名配置（可选）

通过 hosts 解析 `teacherWorkStation` 域名到 `127.0.0.1`，即可使用 `http://teacherWorkStation:5201` 访问：

| 系统     | 脚本                  | 需要权限 |
| -------- | --------------------- | -------- |
| Windows  | 右键以管理员身份运行 `setup-hosts.bat` | 管理员     |

> 脚本支持多次运行（自动跳过已存在的记录），如需恢复原始 hosts 可运行 `setup-hosts.bat remove`。

---

## 📦 迁移到其他电脑

### 方式一：复制整个 `workSystem` 目录
1. 将 `workSystem` 整个文件夹复制到目标机器
2. 确保目标机器已安装 **Node.js 18+**（https://nodejs.org）
3. 运行 `start.bat`，脚本会自动安装依赖并启动

### 方式二：使用 zip 压缩包
压缩包位于 `workSystem.zip`，解压后操作同方式一。

### 方式三：仅迁移 `app/` 源码
1. 复制 `app/` 到任意位置
2. 在该目录执行：
   ```bat
   cd app
   npm install
   npm run dev
   ```

> ✅ **项目使用相对路径**，`app/` 可放在任何目录下、任何盘符下运行。

---

## 📤 数据备份 / 恢复

在「个人中心」可以：
- **导出数据**：所有班级 / 学生 / 成绩 / 笔记 / 考试 / 获奖 / 待办 / AI 配置等打包为 JSON 文件（含版本号）
- **导入数据**：从 JSON 文件恢复（会覆盖当前账号数据，含字段缺失保护）
- **备份历史**：系统每 30 分钟自动备份一次，最多保留 10 个版本。可在「个人中心 → 备份历史」中查看、恢复、下载、删除备份
- **清空数据**：仅清除当前登录人的数据，不影响其他老师

> 数据是**按登录人隔离**的，导出的 JSON 不应给其他账号导入。

---

## 🛠️ 常见问题

**Q: 启动报「未检测到 Node.js」？**
A: 请先安装 Node.js 18+ ：https://nodejs.org/zh-cn/download/

**Q: 端口 5201 被占用？**
A: 使用环境变量 `PORT=xxxx` 指定其他端口（见上文）。

**Q: 依赖安装失败？**
A: 删除 `app/node_modules` 后重新运行 `start.bat`，或检查网络代理。

**Q: 想彻底重装依赖？**
A: 删除 `app/node_modules` 与 `app/package-lock.json` 后重新运行 `start.bat`。

**Q: 浏览器打开是空白？**
A: 查看 `app.log` 是否有错误。常见原因：Node 版本过低 / 端口冲突 / 依赖未装完。

**Q: AI 提示「请先配置 API Key」？**
A: 打开 AI 对话 → 右上角「设置」→ 填入 API Key（推荐 deepseek，便宜好用）。Key 仅保存在本地浏览器，不会上传。

**Q: 不同老师的数据会串吗？**
A: 不会。本系统按登录人**完全隔离**所有数据，详见上文"按登录人数据隔离"章节。

**Q: 双击 start.bat 没反应？**
A: 常见原因：
- Node.js 未安装或版本过低
- 端口 5201 被其他程序占用（脚本会自动尝试清理）
- 系统编码异常：用记事本打开 start.bat 看是否乱码（应正常显示中文）

**Q: 启动后浏览器没自动打开？**
A: 脚本会用系统默认浏览器打开 `http://localhost:5201/`。如果浏览器没动，请手动访问该地址。

---

## 📜 技术栈

- **Vue 3.4** + `<script setup lang="ts">`
- **TypeScript 5.5**
- **Vite 6**（使用相对路径配置）
- **Pinia 2** + localStorage 持久化（按登录人隔离）
- **Vue Router 4**（hash history，游戏页 keepAlive 缓存）
- **Tailwind CSS 3** + 自定义主题（CSS 变量驱动，支持暗色模式 + 4 套主题色）
- **PWA**（vite-plugin-pwa，离线可用，Google Fonts 运行时缓存）
- **Brotli + Gzip 双压缩**
- **lucide-vue-next** 图标
- **dayjs** / **xlsx 0.20.3**（已修复 CVE-2023-30533）/ **nanoid**
- **OpenAI 兼容 API**（默认 deepseek，支持图片多模态）

---

## 📋 v3 更新日志

### P0 紧急修复
- **移动端底部导航修复**：工具 Tab 的 route name 大面积错误导致进入工具页不高亮，改为路由路径前缀匹配
- **数据清空安全修复**：`resetData` 不再使用 `localStorage.clear()`，改为只清除当前用户前缀的 key，避免误删其他老师数据
- **导出/导入字段补全**：新增 exam/award/todo/ai/generated/reward 6 个 store 的数据导出，导入增加字段缺失保护
- **xlsx 安全漏洞修复**：从 0.18.5（CVE-2023-30533 原型污染）升级到 SheetJS 官方 0.20.3
- **PWA 离线字体修复**：添加 Google Fonts 运行时缓存规则，离线不再丢字体；排除 pdf worker/xlsx 等大文件预缓存，首装从 5.7MB 降至 2.1MB

### P1 功能补全
- **备份历史界面**：backup.ts 的自动备份功能（创建/列表/恢复/删除/导出/导入）已接入个人中心界面，用户可可视化管理备份
- **备份容量保护**：createBackup 添加 try-catch，存储空间不足时自动清理旧备份重试

### P2 代码质量
- **内存泄漏修复**：useDebouncedSearch 添加 onUnmounted 清理定时器；AI.vue/Translate.vue/AIImportPanel.vue 的 AbortController 组件卸载时中止
- **未处理 Promise 修复**：AIImportPanel 的 clipboard.writeText 添加 catch
- **下载逻辑统一**：excel.ts/backup.ts/index.ts/StudentManage.vue 的重复下载逻辑统一使用 download.ts 的 downloadBlob
- **SSE 解析统一**：Translate.vue 改为复用 aiCall.ts 的 aiChat 函数

### P3 性能优化
- **Brotli 压缩**：文本资源比 gzip 再省 13-17%（主包 98KB→85KB，xlsx 159KB→133KB）
- **Vendor chunk 拆分**：vue/vue-router/pinia 拆为独立 vendor-vue chunk（112KB），提升长期缓存命中率
- **字体非阻塞加载**：Google Fonts CSS 改为异步加载，不再阻塞首屏渲染
- **路由 keepAlive**：9 款游戏页面切换时保持状态不丢失

### P4 架构改进
- **样式主题色去重**：引入 `--accent-*` 变量体系，三套主题色从各 9 条重复规则合并为 1 份统一规则
- **dark 覆盖瘦身**：删除 3 处仅重复设置自动切换变量的冗余覆盖

---

## 📄 许可证

仅供学习与教学辅助使用。
