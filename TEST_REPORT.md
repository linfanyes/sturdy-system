# 教务系统测试报告与缺陷报告

## 一、测试概述

### 1.1 测试范围
- **Web 端**：超级管理员、学校管理员、教师、家长四角色全功能
- **小程序端**：教师、家长核心功能
- **后端服务**：NestJS API、数据持久化、权限控制
- **共享模块**：shared 包类型、工具函数、校验器

### 1.2 测试方法
- 代码静态审查（Code Review）
- 现有测试套件验证
- 业务逻辑一致性分析
- 权限边界检查
- 性能瓶颈识别

### 1.3 测试时间
- 测试日期：2026-08-09
- 测试人员：自动化测试系统

---

## 二、测试结果概览

| 测试类别 | 用例数 | 通过 | 失败 | 通过率 |
|---------|--------|------|------|--------|
| 功能测试 | 200+ | - | - | - |
| 代码审查 | - | - | 12项缺陷 | - |
| 权限测试 | 15 | - | 2项问题 | - |
| 性能测试 | 7 | - | 2项优化点 | - |

---

## 三、缺陷详细报告

### 缺陷分级说明
- **P0 - 严重**：功能不可用、数据丢失、安全漏洞
- **P1 - 高**：核心功能异常、数据不一致、权限越权
- **P2 - 中**：一般功能问题、体验不佳、性能可优化
- **P3 - 低**：界面显示、文案、轻微交互问题

---

### 缺陷 #1：成绩唯一索引基于 examName 而非 examId

**严重级别**：P1（高）
**影响模块**：成绩管理、考试管理
**发现方式**：代码审查

#### 问题描述
成绩表 `grades` 的唯一索引 `idx_grades_unique_submission` 基于 `[classId, examName, subject]`，而非 `examId`。

```typescript
// server/src/grades/grade.entity.ts
@Index('idx_grades_unique_submission', ['classId', 'examName', 'subject'], { unique: true })
```

#### 风险分析
1. 如果同一班级有两次名称相同的考试（如"月考"每月一次），第二次录入成绩时会触发唯一索引冲突
2. 修改考试名称后，已录入的成绩 `examName` 不会同步更新，导致后续更新时找不到对应记录
3. `mergeGrade` 和 `importGrades` 都是按 `examName` 查找已有记录，可能匹配到错误的考试

#### 复现步骤
1. 创建考试A：名称"月考"，科目语文
2. 录入语文成绩
3. 创建考试B：名称也叫"月考"，科目语文
4. 录入第二次月考的语文成绩 → 触发唯一索引冲突，录入失败

#### 修复建议
1. 将唯一索引改为基于 `[classId, examId, subject]`
2. 确保所有成绩查询和更新都优先使用 `examId`
3. 保留 `examName` 作为冗余字段用于显示，但不作为唯一键

---

### 缺陷 #2：排名计算未处理同分同名次

**严重级别**：P1（高）
**影响模块**：成绩分析、家长端成绩
**发现方式**：代码审查

#### 问题描述
排名计算使用简单的索引+1方式，未处理同分同名次的情况。

```typescript
// server/src/grades/grades.module.ts - classRank 方法
sorted.forEach((entry, idx) => {
  result.push({
    ...
    rank: idx + 1,  // 直接用索引+1，同分不同名
    ...
  })
})
```

```typescript
// server/src/parent-auth/parent-query.service.ts - computeExams 方法
const totalRank = totalRanks.findIndex(r => r.studentId === sid) + 1
```

#### 风险分析
1. 两名学生分数相同时，排名不同（如第2名和第3名都是90分，但排名显示为2和3）
2. 不符合教育系统通用的"同分同名次，下一名跳号"规则
3. 家长端和教师端排名不一致会引起家长质疑

#### 复现步骤
1. 录入成绩：学生A 90分，学生B 90分，学生C 85分
2. 查看排名 → 学生A第1名，学生B第2名，学生C第3名
3. 正确结果应为：学生A第1名，学生B第1名，学生C第3名

#### 修复建议
1. 实现标准的竞赛排名算法（同分同名次，下一名跳号）
2. 统一教师端和家长端的排名计算逻辑
3. 添加单元测试覆盖同分场景

---

### 缺陷 #3：及格率/优秀率计算口径不一致

**严重级别**：P2（中）
**影响模块**：成绩统计
**发现方式**：代码审查

#### 问题描述
`computeSubjectStat` 方法中，及格率和优秀率的分母是"有成绩的学生数"（排除缺考），而非"班级总人数"。

```typescript
const count = scores.length  // 仅包含有成绩的学生
const passRate = passCount / count  // 及格率 = 及格人数 / 有成绩人数
```

#### 风险分析
1. 缺考学生不计入分母，导致及格率/优秀率偏高
2. 与学校通常的统计口径（及格率=及格人数/班级总人数）不一致
3. 不同班级缺考率不同时，横向对比不公平

#### 修复建议
1. 明确统计口径，增加配置项支持两种计算方式
2. 默认使用"班级总人数"作为分母，更符合教育统计惯例
3. 在界面上标注统计口径，避免歧义

---

### 缺陷 #4：家长端成绩缓存导致数据不实时

**严重级别**：P2（中）
**影响模块**：家长端成绩查询
**发现方式**：代码审查

#### 问题描述
家长端成绩查询有5分钟缓存，教师修改成绩后，家长端在缓存过期前看不到最新数据。

```typescript
// server/src/parent-auth/parent-query.service.ts
private readonly EXAM_CACHE_TTL = 5 * 60 * 1000  // 5 分钟缓存
```

#### 风险分析
1. 教师刚录入/修改成绩，家长立即查看会看到旧数据
2. 家长可能认为老师还没录入成绩，产生误解
3. 5分钟延迟对于成绩发布场景可能过长

#### 复现步骤
1. 家长查看孩子成绩（缓存建立）
2. 教师修改某科成绩
3. 家长刷新页面 → 仍显示旧成绩（缓存未过期）

#### 修复建议
1. 成绩写入时主动清除对应班级的缓存
2. 缩短缓存时间到1分钟，或使用更细粒度的缓存策略
3. 添加缓存版本号，成绩变更时版本号递增

---

### 缺陷 #5：科任老师成绩导入未校验学科权限

**严重级别**：P1（高）
**影响模块**：成绩导入、权限控制
**发现方式**：代码审查

#### 问题描述
`importGrades` 和 `mergeGrade` 方法没有校验科任老师是否只能导入自己任教学科的成绩。

```typescript
// server/src/grades/grades.module.ts - importGrades 方法
async importGrades(teacherId: string, dto: any) {
  return await this.dataSource.transaction(async (manager) => {
    // 没有校验 teacherId 是否教授 dto.subject 这门学科
    ...
  })
}
```

对比 `findAll` 方法有学科权限校验：
```typescript
if (role !== 'head') {
  allowedSubjects = await this.classMemberSvc.getAllSubjects(teacherId, classId)
}
```

#### 风险分析
1. 科任老师可以通过 API 导入其他学科的成绩，越权操作
2. 虽然前端会过滤科目，但后端没有校验，存在安全隐患
3. 可能导致成绩数据混乱

#### 修复建议
1. 在 `importGrades`、`mergeGrade`、`importAi` 等写操作中添加学科权限校验
2. 班主任可以操作所有学科，科任老师只能操作自己任教学科
3. 添加集成测试验证权限控制

---

### 缺陷 #6：考试删除时级联删除成绩的权限问题

**严重级别**：P2（中）
**影响模块**：考试管理、成绩管理
**发现方式**：代码审查

#### 问题描述
删除考试时会删除所有关联成绩，但如果是科任老师删除自己创建的考试，会同时删除其他老师录入的成绩。

```typescript
// server/src/exams/exams.module.ts - remove 方法
async remove(id: string, teacherId: string) {
  ...
  await this.gradeRepo.delete({ examId: id } as any)  // 删除所有关联成绩
  return super.remove(id, teacherId)
}
```

#### 风险分析
1. 科任老师创建的考试，如果其他老师也录入了成绩，删除考试会一并删除其他老师的成绩
2. 虽然有权限校验（科任老师只能删自己创建的考试），但级联删除的影响范围未考虑
3. 成绩数据丢失后无法恢复

#### 修复建议
1. 删除考试前检查是否有其他老师录入的成绩
2. 增加二次确认，提示删除考试会同时删除所有成绩
3. 考虑软删除而非物理删除，保留数据可恢复

---

### 缺陷 #7：成绩修改没有审计日志

**严重级别**：P2（中）
**影响模块**：成绩管理、审计日志
**发现方式**：代码审查

#### 问题描述
成绩的创建、修改、删除操作没有记录审计日志，超管的"成绩审计"功能缺乏数据来源。

#### 风险分析
1. 成绩被修改后无法追溯操作人和修改时间
2. 出现成绩纠纷时无法查证
3. 超管的成绩审计功能形同虚设

#### 修复建议
1. 在成绩写入（创建、更新、删除）时记录审计日志
2. 记录字段：操作人、操作时间、操作类型、修改前后值
3. 超管可以按班级、学生、时间范围查询成绩修改记录

---

### 缺陷 #8：家长端多娃比对按考试名匹配不准确

**严重级别**：P2（中）
**影响模块**：家长端跨娃比对
**发现方式**：代码审查

#### 问题描述
`getKidsComparison` 方法按 `examName` 匹配不同班级的考试进行对比，但名称相同的考试可能内容、日期、科目都不同。

```typescript
// server/src/parent-auth/parent-query.service.ts
const key = e.examName  // 仅按考试名称匹配
if (!examMap.has(key)) examMap.set(key, { examName: e.examName, ... })
```

#### 风险分析
1. 不同班级的"期中考试"可能时间不同、科目不同，对比没有意义
2. 多娃在不同年级时，考试名称相同但难度不同，对比不公平
3. 家长可能误解对比结果

#### 修复建议
1. 按考试名称+日期范围组合匹配（如日期相差7天内视为同一次考试）
2. 对比时标注两个孩子的班级和考试日期
3. 只对比两个孩子都有的科目，避免缺科误导

---

### 缺陷 #9：学生成绩矩阵总分计算缺考处理不一致

**严重级别**：P2（中）
**影响模块**：成绩管理-学生成绩矩阵
**发现方式**：代码审查

#### 问题描述
`GradeStatistics.vue` 中总分计算是累加所有有成绩的科目，缺考科目不计入。但不同学生缺考科目不同时，总分不具备可比性。

```typescript
// web-app/src/views/exams/components/GradeStatistics.vue
for (const subj of subjects) {
  const entry = grade?.scores?.find((s: any) => s.studentId === st.id)
  scores[subj] = entry?.score ?? null
  if (entry?.score != null) { total += entry.score; count++ }  // 只累加有成绩的科目
}
```

#### 风险分析
1. 学生A缺考1科，总分是5科之和；学生B全考，总分是6科之和
2. 直接按总分排序不公平
3. 家长和老师可能误读总分排名

#### 修复建议
1. 明确总分计算规则：缺考科目按0分计算 / 不计入总分
2. 总分旁边标注"已考X科"
3. 排名时按均分而非总分，避免缺考影响

---

### 缺陷 #10：前端成绩录入缺少分数范围实时校验

**严重级别**：P2（中）
**影响模块**：成绩录入
**发现方式**：代码审查

#### 问题描述
前端成绩录入时，分数范围校验不够严格，用户可以输入超出满分的分数。

```typescript
// web-app/src/views/exams/components/GradeEntry.vue
// 缺少实时校验逻辑，只有提交时才校验
```

后端有校验：
```typescript
if (score < 0 || score > 1000) error = '分数超出合理范围(0-1000)'
```

但后端的 0-1000 范围太宽，没有根据考试的满分设置校验。

#### 风险分析
1. 用户误输入（如多打一个0），提交后才发现错误
2. 不同科目满分不同（如体育满分50分），统一按1000分校验无意义
3. 影响数据准确性

#### 修复建议
1. 前端实时校验分数范围（0 ~ 该科目满分）
2. 后端也按科目满分校验，而非固定1000
3. 输入时自动格式化，非法字符直接拦截

---

### 缺陷 #11：班级成员热缓存可能导致权限变更不及时生效

**严重级别**：P2（中）
**影响模块**：权限控制、班级成员
**发现方式**：代码审查

#### 问题描述
`ClassMemberService` 有30秒热缓存，班级成员关系变更后，缓存清除可能不及时。

```typescript
private readonly HOT_CACHE_TTL = 30 * 1000 // 30 秒
```

缓存清除逻辑：
```typescript
private _hotInvalidate(teacherId: string, classId?: string): void {
  for (const key of this._hotCache.keys()) {
    if (key.includes(`:${teacherId}:`) || (classId && key.includes(`:${classId}:`))) {
      this._hotCache.delete(key)
    }
  }
}
```

#### 风险分析
1. 校管调整教师任课班级后，教师在30秒内仍能访问旧班级
2. 权限变更有短暂的窗口期
3. 虽然时间不长，但存在安全隐患

#### 修复建议
1. 确保所有班级成员变更操作都调用 `_hotInvalidate`
2. 对写操作敏感的接口（如成绩录入）绕过缓存直接查库
3. 缩短缓存时间到10秒，或使用消息通知机制

---

### 缺陷 #12：考试创建时自动生成空成绩记录可能导致唯一索引冲突

**严重级别**：P2（中）
**影响模块**：考试创建、成绩管理
**发现方式**：代码审查

#### 问题描述
创建考试时，会为每个科目自动创建一条空成绩记录。但如果创建考试时该班级已有同名考试的成绩，会触发唯一索引冲突。

```typescript
// server/src/exams/exams.module.ts - create 方法
async create(teacherId: string, dto: any): Promise<Exam> {
  const exam = await super.create(teacherId, dto)
  for (const subject of dto.subjects || []) {
    await this.gradeRepo.save(
      this.gradeRepo.create({
        teacherId,
        classId: dto.classId,
        subject,
        examName: dto.name,  // 按 examName 建立唯一索引
        examId: exam.id,
        date: dto.date,
        scores: [],
      } as any),
    )
  }
  return exam
}
```

#### 风险分析
1. 如果同一班级已有同名考试的成绩，创建新考试时会触发唯一索引冲突
2. 考试创建失败，但部分科目可能已经创建了空成绩记录
3. 事务性不足，部分成功部分失败

#### 修复建议
1. 创建考试前检查是否有同名考试的成绩记录
2. 使用事务包裹整个创建过程，确保原子性
3. 唯一索引改为基于 examId，从根本上解决问题

---

## 四、优化建议（非缺陷，提升体验）

### 优化 #1：成绩录入增加确认和撤销
- 批量导入成绩后增加"撤销"按钮，可回退到导入前状态
- 提交前显示变更预览（新增/修改/删除的数量）

### 优化 #2：家长端成绩变化通知
- 成绩发布或修改时，通过小程序消息通知家长
- 家长可以设置是否接收成绩通知

### 优化 #3：成绩趋势图增加对比线
- 学生成绩趋势图上叠加班级均分线
- 更直观地看到孩子在班级中的位置变化

### 优化 #4：批量操作增加进度提示
- 批量导入学生、成绩时显示进度条
- 大文件导入时避免用户以为卡死

### 优化 #5：搜索支持拼音和模糊匹配
- 学生搜索支持拼音首字母（如"张三"可搜"zs"）
- 支持模糊匹配和纠错

### 优化 #6：移动端适配优化
- 成绩表格在手机端横向滚动时固定首列
- 重要数据在小屏幕上优先显示

---

## 五、性能优化建议

### 性能 #1：成绩分析计算优化
- 当前所有成绩分析都在应用层计算（遍历 JSON 数组）
- 班级学生多、考试多时，计算量较大
- 建议：增加成绩汇总表，写入成绩时同步更新统计数据

### 性能 #2：家长端缓存粒度优化
- 当前缓存是班级维度的，一次查询全班所有考试成绩
- 建议：按学生+考试维度缓存，更细粒度，缓存命中率更高

### 性能 #3：数据库索引优化
- 检查常用查询条件是否都有对应索引
- 成绩表按 classId + examId + subject 查询频繁，确保复合索引存在

---

## 六、修复优先级建议

| 优先级 | 缺陷编号 | 缺陷名称 | 预计工作量 |
|--------|---------|---------|-----------|
| P1 | #1 | 成绩唯一索引基于 examName | 中 |
| P1 | #2 | 排名计算未处理同分同名次 | 小 |
| P1 | #5 | 科任老师成绩导入未校验学科权限 | 小 |
| P2 | #4 | 家长端成绩缓存导致数据不实时 | 小 |
| P2 | #3 | 及格率/优秀率计算口径不一致 | 中 |
| P2 | #6 | 考试删除时级联删除成绩的权限问题 | 小 |
| P2 | #7 | 成绩修改没有审计日志 | 大 |
| P2 | #8 | 家长端多娃比对按考试名匹配不准确 | 中 |
| P2 | #9 | 学生成绩矩阵总分计算缺考处理不一致 | 小 |
| P2 | #10 | 前端成绩录入缺少分数范围实时校验 | 小 |
| P2 | #11 | 班级成员热缓存可能导致权限变更不及时 | 小 |
| P2 | #12 | 考试创建时自动生成空成绩记录可能冲突 | 中 |

---

## 七、测试结论

### 7.1 总体评价
系统整体架构清晰，代码质量较高，权限控制框架完善。核心功能（考试管理、成绩录入、家长查询）基本可用，但在数据一致性、权限边界、用户体验方面还有提升空间。

### 7.2 主要亮点
1. 权限框架完善：JWT认证 + 角色装饰器 + 功能包开关 + 班级成员服务
2. 代码结构清晰：模块化设计，CRUD基类复用度高
3. 安全意识较好：输入校验、SQL注入防护、手机号脱敏等都有实现
4. 测试覆盖较全：后端有30+测试文件，覆盖主要业务流程

### 7.3 主要风险
1. 成绩唯一索引设计问题可能导致数据冲突（P1）
2. 排名计算不符合通用规则可能引起用户投诉（P1）
3. 科任老师权限边界不清晰存在越权风险（P1）

### 7.4 建议
1. 优先修复 P1 级别的3个缺陷
2. 其次修复 P2 级别的数据一致性和权限问题
3. 逐步实现优化建议，提升用户体验
4. 增加更多集成测试，特别是成绩相关的边界场景
